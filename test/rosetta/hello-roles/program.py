import os

def fetch(url, timeout=1.0):
    for attempt in range(3):
        while not done:
            break
    return [x for x in items if x]

class Config:
    def method(self):
        value = obj.attr
