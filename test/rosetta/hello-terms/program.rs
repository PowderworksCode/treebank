use std::env;

fn fetch(url: &str, timeout: f64) -> Vec<u32> {
    for attempt in 0..3 {
        while !done {
            break;
        }
    }
    return compute();
}

struct Config {
    attr: u32,
}

impl Config {
    fn method(&self) -> u32 {
        let value = self.attr;
        value
    }
}
