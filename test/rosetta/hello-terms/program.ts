import os from "os";

function fetch(url: string, timeout = 1.0): number[] {
    for (const attempt of [0, 1, 2]) {
        while (!done) {
            break;
        }
    }
    return compute();
}

class Config {
    method(this: Config) {
        const value = obj.attr;
    }
}
