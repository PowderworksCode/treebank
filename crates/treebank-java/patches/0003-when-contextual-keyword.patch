diff --git a/grammar.js b/grammar.js
index 072f734..01bfa0c 100644
--- a/grammar.js
+++ b/grammar.js
@@ -87,6 +87,11 @@ module.exports = grammar({
     [$.inferred_parameters, $.primary_expression],
     [$.argument_list, $.record_pattern_body],
     [$.yield_statement, $._reserved_identifier],
+    // `when` is both a guard keyword and a legal identifier, so inside a
+    // switch label `case x instanceof T when …` could bind `when` as the
+    // instanceof pattern's name or start the guard. GLR keeps both alive
+    // and the one that parses wins.
+    [$.instanceof_expression],
   ],
 
   word: $ => $.identifier,
@@ -1283,6 +1288,10 @@ module.exports = grammar({
           'record',
           'with',
           'sealed',
+          // `when` is a contextual keyword (JLS 3.9): it introduces a switch
+          // guard but is still a legal identifier everywhere, including a
+          // for-init — `for (SimpleWhen when = this.when; ...)`.
+          'when',
         ),
         $.identifier,
       )),
diff --git a/test/corpus/expressions.txt b/test/corpus/expressions.txt
index 40e06e3..f6d8931 100644
--- a/test/corpus/expressions.txt
+++ b/test/corpus/expressions.txt
@@ -1951,3 +1951,81 @@ void foo(int @Foo ... x) {
         (variable_declarator
           (identifier))))
     (block)))
+
+================================================================================
+`when` is a contextual keyword, still legal as an identifier
+================================================================================
+
+class A {
+  int when = 1;
+  void f() {
+    for (Object when = null; when != null; when = null) {}
+  }
+  String g(Object o) {
+    return switch (o) {
+      case Integer i when i > 0 -> "pos";
+      default -> "other";
+    };
+  }
+}
+
+---
+
+(program
+  (class_declaration
+    (identifier)
+    (class_body
+      (field_declaration
+        (integral_type)
+        (variable_declarator
+          (identifier)
+          (decimal_integer_literal)))
+      (method_declaration
+        (void_type)
+        (identifier)
+        (formal_parameters)
+        (block
+          (for_statement
+            (local_variable_declaration
+              (type_identifier)
+              (variable_declarator
+                (identifier)
+                (null_literal)))
+            (binary_expression
+              (identifier)
+              (null_literal))
+            (assignment_expression
+              (identifier)
+              (null_literal))
+            (block))))
+      (method_declaration
+        (type_identifier)
+        (identifier)
+        (formal_parameters
+          (formal_parameter
+            (type_identifier)
+            (identifier)))
+        (block
+          (return_statement
+            (switch_expression
+              (parenthesized_expression
+                (identifier))
+              (switch_block
+                (switch_rule
+                  (switch_label
+                    (pattern
+                      (type_pattern
+                        (type_identifier)
+                        (identifier)))
+                    (guard
+                      (binary_expression
+                        (identifier)
+                        (decimal_integer_literal))))
+                  (expression_statement
+                    (string_literal
+                      (string_fragment))))
+                (switch_rule
+                  (switch_label)
+                  (expression_statement
+                    (string_literal
+                      (string_fragment))))))))))))
