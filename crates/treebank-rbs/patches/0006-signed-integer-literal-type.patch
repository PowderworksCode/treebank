diff --git a/grammar.js b/grammar.js
index 8286f75..ae21dd2 100644
--- a/grammar.js
+++ b/grammar.js
@@ -185,7 +185,12 @@ module.exports = grammar({
       /:((\.\.)|(\|)|(\^)|(&)|(<=>)|(==)|(===)|(=~)|(>)|(>=)|(<)|(<=)|(\+)|(-)|(\*)|(\/)|(%)|(!)|(!~)|(\*\*)|(<<)|(>>)|(~)|(\+@)|(-@)|(~@)|(\[\])|(\[\]=)|(`))/,
     )),
 
-    integer_literal: $ => /\d(_?\d)*/,
+    // An integer literal type may be signed. rbs's own core signatures use it
+    // for comparison results — `def <=>: (untyped) -> (-1 | 0 | 1)` — and
+    // language_server-protocol declares its error codes as negative constants
+    // (`PARSE_ERROR: -32700`). The sign is part of the literal: a type
+    // position has no unary minus to fall back on.
+    integer_literal: $ => /[+-]?\d(_?\d)*/,
 
     // https://ruby-doc.org/core-2.5.0/doc/syntax/literals_rdoc.html#label-Strings
     escape_sequence: $ => token(seq(
diff --git a/test/corpus/types.txt b/test/corpus/types.txt
index ce331a1..3fa901b 100644
--- a/test/corpus/types.txt
+++ b/test/corpus/types.txt
@@ -600,3 +600,29 @@ end
         (const_decl (const_name (constant))
           (type (class_type (class_name (constant))
             (type_arguments (type (class_type (class_name (constant)))))))))))) 
+
+================================================================================
+signed integer literal type
+================================================================================
+
+class C
+  X: -32700
+  def f: () -> (-1 | 0 | 1)
+end
+
+--------------------------------------------------------------------------------
+
+(program
+  (decl
+    body: (class_decl
+      name: (class_name (constant))
+      body: (members
+        (const_decl (const_name (constant)) (type (integer_literal)))
+        (member body: (method_member
+          name: (method_name (identifier))
+          (method_types (method_type body: (method_type_body (parameters)
+            (type (union_type
+              left: (type (union_type
+                left: (type (integer_literal))
+                right: (type (integer_literal))))
+              right: (type (integer_literal)))))))))))))
