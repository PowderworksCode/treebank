diff --git a/grammar.js b/grammar.js
index be46444..58bccb1 100644
--- a/grammar.js
+++ b/grammar.js
@@ -492,8 +492,11 @@ module.exports = grammar({
       seq("alias", field("name", $.singleton_method_name), field("origin_name", $.singleton_method_name)),
     ),
 
-    ivar_name: $ => /@[a-zA-Z]\w*/,
-    cvar_name: $ => /@@[a-zA-Z]\w+/,
+    // An instance or class variable name may begin with an underscore, as it
+    // may in Ruby: `@_memoized_method_cache`. Only a letter was accepted, so
+    // a module declaring one failed entirely.
+    ivar_name: $ => /@[a-zA-Z_]\w*/,
+    cvar_name: $ => /@@[a-zA-Z_]\w+/,
 
     operator: $ => choice(
       '..', '|', '^', '&', '<=>', '==', '===', '=~', '>', '>=', '<', '<=', '+',
diff --git a/test/corpus/decls.txt b/test/corpus/decls.txt
index e5fd7b4..91a66fe 100644
--- a/test/corpus/decls.txt
+++ b/test/corpus/decls.txt
@@ -456,3 +456,22 @@ end
             (method_types (method_type body: (method_type_body
               (parameters)
               (type (class_type (class_name (constant)))))))))))))
+
+================================================================================
+instance variable name beginning with an underscore
+================================================================================
+
+module M
+  @_cache: Integer?
+end
+
+--------------------------------------------------------------------------------
+
+(program
+  (decl
+    body: (module_decl
+      name: (module_name (constant))
+      body: (members
+        (member body: (ivar_member
+          name: (ivar_name)
+          (type (optional_type (type (class_type (class_name (constant))))))))))))
