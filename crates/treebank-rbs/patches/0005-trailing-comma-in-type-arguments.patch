diff --git a/grammar.js b/grammar.js
index 678b344..8286f75 100644
--- a/grammar.js
+++ b/grammar.js
@@ -155,7 +155,10 @@ module.exports = grammar({
       seq(optional($.namespace), $.constant, $._scope),
     )),
 
-    type_arguments: $ => seq("[", commaSep1($.type), "]"),
+    // A trailing comma is allowed here, as it already is in record and tuple
+    // types. aws-sdk's generated signatures wrap long argument lists and
+    // leave one behind: `Array[\n  { ... },\n]`.
+    type_arguments: $ => seq("[", commaSep1($.type), optional(","), "]"),
 
     _literal: $ => choice(
       $.string_literal,
diff --git a/test/corpus/types.txt b/test/corpus/types.txt
index 0d92635..ce331a1 100644
--- a/test/corpus/types.txt
+++ b/test/corpus/types.txt
@@ -581,3 +581,22 @@ FOO: (String | Integer) & Float
             (class_type
               (class_name
                 (constant)))))))))
+
+================================================================================
+trailing comma in type arguments
+================================================================================
+
+class C
+  Y: Array[Integer,]
+end
+
+--------------------------------------------------------------------------------
+
+(program
+  (decl
+    body: (class_decl
+      name: (class_name (constant))
+      body: (members
+        (const_decl (const_name (constant))
+          (type (class_type (class_name (constant))
+            (type_arguments (type (class_type (class_name (constant)))))))))))) 
