diff --git a/common/define-grammar.js b/common/define-grammar.js
index ebda6cc..3e6ef2e 100644
--- a/common/define-grammar.js
+++ b/common/define-grammar.js
@@ -821,6 +821,9 @@ module.exports = function defineGrammar(dialect) {
         '`',
         repeat(choice(
           alias($._template_chars, $.string_fragment),
+          // `${infer L}$\{${infer R}\}` — a template literal type is a
+          // template literal, escapes and all
+          $.escape_sequence,
           $.template_type,
         )),
         '`',
diff --git a/test/corpus/types.txt b/test/corpus/types.txt
index 35a9138..0617f42 100644
--- a/test/corpus/types.txt
+++ b/test/corpus/types.txt
@@ -2404,3 +2404,25 @@ declare const ansiRegex: ({ onlyFirst }?: {
                       type: (type_annotation
                         (predefined_type)))))))
             return_type: (type_identifier)))))))
+
+==================================
+Escape sequences in a template literal type
+==================================
+
+type A = `${infer L}$\{${infer R}\}`;
+
+---
+
+(program
+  (type_alias_declaration
+    name: (type_identifier)
+    value: (template_literal_type
+      (template_type
+        (infer_type
+          (type_identifier)))
+      (string_fragment)
+      (escape_sequence)
+      (template_type
+        (infer_type
+          (type_identifier)))
+      (escape_sequence))))
