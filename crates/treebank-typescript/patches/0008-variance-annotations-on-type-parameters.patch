diff --git a/common/define-grammar.js b/common/define-grammar.js
index c7f8daa..f41e127 100644
--- a/common/define-grammar.js
+++ b/common/define-grammar.js
@@ -1036,6 +1036,9 @@ module.exports = function defineGrammar(dialect) {
 
       type_parameter: $ => seq(
         optional('const'),
+        // TS 4.7 variance annotations: `in T`, `out T`, `in out T`
+        optional('in'),
+        optional('out'),
         field('name', $._type_identifier),
         field('constraint', optional($.constraint)),
         field('value', optional($.default_type)),
diff --git a/test/corpus/types.txt b/test/corpus/types.txt
index 48a6a9e..b3c5d92 100644
--- a/test/corpus/types.txt
+++ b/test/corpus/types.txt
@@ -2283,3 +2283,23 @@ declare let b: import("x").Y[];
                   (string
                     (string_fragment))))
               property: (property_identifier))))))))
+
+==================================
+Variance annotations on type parameters
+==================================
+
+interface A<in T, out U, in out V> {}
+
+---
+
+(program
+  (interface_declaration
+    name: (type_identifier)
+    type_parameters: (type_parameters
+      (type_parameter
+        name: (type_identifier))
+      (type_parameter
+        name: (type_identifier))
+      (type_parameter
+        name: (type_identifier)))
+    body: (interface_body)))
