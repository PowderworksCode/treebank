diff --git a/common/define-grammar.js b/common/define-grammar.js
index 553e09e..ebda6cc 100644
--- a/common/define-grammar.js
+++ b/common/define-grammar.js
@@ -710,7 +710,7 @@ module.exports = function defineGrammar(dialect) {
 
       omitting_type_annotation: $ => seq('-?:', $.type),
       adding_type_annotation: $ => seq('+?:', $.type),
-      opting_type_annotation: $ => seq('?:', $.type),
+      opting_type_annotation: $ => seq('?', ':', $.type),
       type_annotation: $ => seq(
         ':',
         $.type,
diff --git a/test/corpus/types.txt b/test/corpus/types.txt
index 662225b..bf53e6e 100644
--- a/test/corpus/types.txt
+++ b/test/corpus/types.txt
@@ -2355,3 +2355,23 @@ type A = { [type in keyof T]?: string };
             (type_identifier)))
         type: (opting_type_annotation
           (predefined_type))))))
+
+==================================
+Space between ? and : in an optional mapped type
+==================================
+
+type A = { [K in keyof T]? : string };
+
+---
+
+(program
+  (type_alias_declaration
+    name: (type_identifier)
+    value: (object_type
+      (index_signature
+        (mapped_type_clause
+          name: (type_identifier)
+          type: (index_type_query
+            (type_identifier)))
+        type: (opting_type_annotation
+          (predefined_type))))))
