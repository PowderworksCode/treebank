diff --git a/common/define-grammar.js b/common/define-grammar.js
index efb0b76..6899785 100644
--- a/common/define-grammar.js
+++ b/common/define-grammar.js
@@ -322,8 +322,13 @@ module.exports = function defineGrammar(dialect) {
         seq(
           'export',
           'type',
-          $.export_clause,
-          optional($._from_clause),
+          choice(
+            // `export type * [as ns] from '...'` (TS 5.0), same shape as the
+            // base grammar's `export *` variants.
+            seq('*', $._from_clause),
+            seq($.namespace_export, $._from_clause),
+            seq($.export_clause, optional($._from_clause)),
+          ),
           $._semicolon,
         ),
         seq('export', '=', $.expression, $._semicolon),
diff --git a/test/corpus/declarations.txt b/test/corpus/declarations.txt
index 75d38d2..d6d25a6 100644
--- a/test/corpus/declarations.txt
+++ b/test/corpus/declarations.txt
@@ -1467,3 +1467,22 @@ class Bar extends Foo {
         name: (property_identifier)
         parameters: (formal_parameters)
         body: (statement_block)))))
+
+==================================
+Export type star from
+==================================
+
+export type * from './primitive.d.ts';
+export type * as ns from './x.d.ts';
+
+---
+
+(program
+  (export_statement
+    source: (string
+      (string_fragment)))
+  (export_statement
+    (namespace_export
+      (identifier))
+    source: (string
+      (string_fragment))))
