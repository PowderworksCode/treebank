diff --git a/common/define-grammar.js b/common/define-grammar.js
index 1748e20..553e09e 100644
--- a/common/define-grammar.js
+++ b/common/define-grammar.js
@@ -942,7 +942,12 @@ module.exports = function defineGrammar(dialect) {
       ),
 
       mapped_type_clause: $ => seq(
-        field('name', $._type_identifier),
+        // `[type in keyof T]` — contextual keywords are valid key names,
+        // as they already are in the `[k: string]` branch of index_signature
+        field('name', choice(
+          $._type_identifier,
+          alias($._reserved_identifier, $.type_identifier),
+        )),
         'in',
         field('type', $.type),
         optional(seq('as', field('alias', $.type))),
diff --git a/test/corpus/types.txt b/test/corpus/types.txt
index 97ceec8..662225b 100644
--- a/test/corpus/types.txt
+++ b/test/corpus/types.txt
@@ -2335,3 +2335,23 @@ declare const schema: (import('./types').CollectionTag | import('./types').Scala
                       (string
                         (string_fragment))))
                   property: (property_identifier))))))))))
+
+==================================
+Contextual keyword as a mapped type key
+==================================
+
+type A = { [type in keyof T]?: string };
+
+---
+
+(program
+  (type_alias_declaration
+    name: (type_identifier)
+    value: (object_type
+      (index_signature
+        (mapped_type_clause
+          name: (type_identifier)
+          type: (index_type_query
+            (type_identifier)))
+        type: (opting_type_annotation
+          (predefined_type))))))
