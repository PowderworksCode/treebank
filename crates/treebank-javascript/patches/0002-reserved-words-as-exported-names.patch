diff --git a/grammar.js b/grammar.js
index 7ca23ff..fdb8183 100644
--- a/grammar.js
+++ b/grammar.js
@@ -195,8 +195,14 @@ module.exports = grammar({
       ),
     ),
 
+    // `export * as import from "m"` — the exported name is an
+    // IdentifierName, same as the alias in an export_specifier.
     namespace_export: $ => seq(
-      '*', 'as', $._module_export_name,
+      '*', 'as', reserved('properties', choice(
+        $.identifier,
+        $.string,
+        'default',
+      )),
     ),
 
     export_clause: $ => seq(
@@ -206,11 +212,20 @@ module.exports = grammar({
       '}',
     ),
 
+    // The exported name after `as` is a ModuleExportName, i.e. an
+    // IdentifierName: reserved words are legal there. The name before `as`
+    // is left reserved on purpose — without a from-clause an early error
+    // requires it to be an IdentifierReference, so `export { import }`
+    // must keep failing (test/negative/export-reserved-local-name.mjs).
     export_specifier: $ => seq(
       field('name', $._module_export_name),
       optional(seq(
         'as',
-        field('alias', $._module_export_name),
+        field('alias', reserved('properties', choice(
+          $.identifier,
+          $.string,
+          'default',
+        ))),
       )),
     ),
 
diff --git a/test/corpus/statements.txt b/test/corpus/statements.txt
index 81b3c80..fa6f806 100644
--- a/test/corpus/statements.txt
+++ b/test/corpus/statements.txt
@@ -1462,3 +1462,33 @@ let x = { a: //  3
           (property_identifier)
           (comment)
           (number))))))
+
+============================================
+Exported names may be reserved words
+============================================
+
+export { _import as import };
+export { fn as class, obj as new };
+export * as import from 'foo';
+
+----
+
+(program
+  (export_statement
+    (export_clause
+      (export_specifier
+        name: (identifier)
+        alias: (identifier))))
+  (export_statement
+    (export_clause
+      (export_specifier
+        name: (identifier)
+        alias: (identifier))
+      (export_specifier
+        name: (identifier)
+        alias: (identifier))))
+  (export_statement
+    (namespace_export
+      (identifier))
+    source: (string
+      (string_fragment))))
