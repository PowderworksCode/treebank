// The scanner is shared across the Python variants; this stub is what
// `tree-sitter generate` and `build.rs` expect to find in a grammar's own
// `src/`. See ../../common/scanner.c.
#include "../../common/scanner.c"
