diff --git a/grammar.js b/grammar.js
index 7ec1c43..90a6f14 100644
--- a/grammar.js
+++ b/grammar.js
@@ -88,6 +88,17 @@ module.exports = grammar({
     ')',
     '}',
     'except',
+
+    // The `:` that opens an f-string format specifier. It has to come from
+    // the external scanner because the ordinary lexer sees `:=` — one token,
+    // longer than `:` — and takes it, so every specifier whose first
+    // character is `=` (`f"{v:=>10}"`, sign-aware zero padding) failed to
+    // parse. The scanner emits this only when the parser says a format
+    // specifier may start here, so it cannot affect `{1, x := 2}` in a set
+    // literal, where this token is not valid. Raising the colon's lexical
+    // precedence instead does not work: tree-sitter merges tokens with the
+    // same lexeme, so the bump is global and breaks that set literal.
+    $._format_spec_colon,
   ],
 
   inline: $ => [
@@ -1141,7 +1152,10 @@ module.exports = grammar({
     _not_escape_sequence: _ => token.immediate('\\'),
 
     format_specifier: $ => seq(
-      ':',
+      // The ordinary ':' handles every specifier; the external token exists
+      // only for the one the ordinary lexer cannot produce, where the
+      // specifier's first character is '=' and the lexer takes `:=`.
+      choice(':', $._format_spec_colon),
       repeat(choice(
         token(prec(1, /[^{}\n]+/)),
         alias($.interpolation, $.format_expression),
diff --git a/src/scanner.c b/src/scanner.c
index 24136a5..34966b8 100644
--- a/src/scanner.c
+++ b/src/scanner.c
@@ -19,6 +19,7 @@ enum TokenType {
     CLOSE_BRACKET,
     CLOSE_BRACE,
     EXCEPT,
+    FORMAT_SPEC_COLON,
 };
 
 typedef enum {
@@ -98,6 +99,29 @@ bool tree_sitter_python_external_scanner_scan(void *payload, TSLexer *lexer, con
     bool error_recovery_mode = valid_symbols[STRING_CONTENT] && valid_symbols[INDENT];
     bool within_brackets = valid_symbols[CLOSE_BRACE] || valid_symbols[CLOSE_PAREN] || valid_symbols[CLOSE_BRACKET];
 
+    // The colon that opens an f-string format specifier. Emitted here, ahead
+    // of the ordinary lexer, because that lexer would take `:=` — a longer
+    // token — and every specifier starting with `=` would fail. valid_symbols
+    // carries the parser's own answer to "may a format specifier start
+    // here", so this cannot fire anywhere else, and in particular not in a
+    // set literal where `x := 2` is genuinely a walrus.
+    // Only when the very next character is '=', which is the sole case the
+    // ordinary lexer gets wrong: it takes `:=` as one token and the specifier
+    // never starts. Every other specifier lexes correctly as a plain ':', and
+    // claiming those here would steal the colons that belong to a slice, a
+    // dict or a lambda inside the same replacement field — measured, that
+    // cost 6 extra gap files.
+    if (valid_symbols[FORMAT_SPEC_COLON] && lexer->lookahead == ':') {
+        lexer->mark_end(lexer);
+        advance(lexer);
+        if (lexer->lookahead == '=') {
+            lexer->mark_end(lexer);
+            lexer->result_symbol = FORMAT_SPEC_COLON;
+            return true;
+        }
+        return false;
+    }
+
     bool advanced_once = false;
     if (valid_symbols[ESCAPE_INTERPOLATION] && scanner->delimiters.size > 0 &&
         (lexer->lookahead == '{' || lexer->lookahead == '}') && !error_recovery_mode) {
diff --git a/test/corpus/expressions.txt b/test/corpus/expressions.txt
index db90afd..2bc733a 100644
--- a/test/corpus/expressions.txt
+++ b/test/corpus/expressions.txt
@@ -1295,3 +1295,25 @@ u = tuple[*Ts]
       right: (subscript
         value: (identifier)
         subscript: (list_splat (identifier))))))
+
+==================================
+Format specifier beginning with an equals sign
+==================================
+
+a = f"{v:=>10}"
+b = f"{num:=.2Uf}"
+c = f"{v:>10}"
+d = f"{(v := 10)}"
+
+---
+
+(module
+  (expression_statement (assignment left: (identifier) right: (string (string_start)
+    (interpolation expression: (identifier) format_specifier: (format_specifier)) (string_end))))
+  (expression_statement (assignment left: (identifier) right: (string (string_start)
+    (interpolation expression: (identifier) format_specifier: (format_specifier)) (string_end))))
+  (expression_statement (assignment left: (identifier) right: (string (string_start)
+    (interpolation expression: (identifier) format_specifier: (format_specifier)) (string_end))))
+  (expression_statement (assignment left: (identifier) right: (string (string_start)
+    (interpolation expression: (parenthesized_expression
+      (named_expression name: (identifier) value: (integer)))) (string_end)))))
