diff --git a/src/scanner.c b/src/scanner.c
index 1fc77cd..2a619c5 100644
--- a/src/scanner.c
+++ b/src/scanner.c
@@ -227,8 +227,9 @@ bool tree_sitter_python_external_scanner_scan(void *payload, TSLexer *lexer, con
         } else if (lexer->lookahead == '\t') {
             indent_length += 8;
             skip(lexer);
-        } else if (lexer->lookahead == '#' && (valid_symbols[INDENT] || valid_symbols[DEDENT] ||
-                                               valid_symbols[NEWLINE] || valid_symbols[EXCEPT])) {
+        } else if (lexer->lookahead == '#' &&
+                   (found_end_of_line || valid_symbols[INDENT] || valid_symbols[DEDENT] ||
+                    valid_symbols[NEWLINE] || valid_symbols[EXCEPT])) {
             // If we haven't found an EOL yet,
             // then this is a comment after an expression:
             //   foo = bar # comment
diff --git a/test/corpus/statements.txt b/test/corpus/statements.txt
index 3f8c29f..3c73e01 100644
--- a/test/corpus/statements.txt
+++ b/test/corpus/statements.txt
@@ -1626,3 +1626,27 @@ def more_generic[T, *Ts, **P]():
                               (identifier)))))))))))))
       (return_statement
         (identifier)))))
+
+==================================
+Comment at column zero inside a block
+==================================
+
+class C:
+    @property
+# a comment, unindented
+    def f(self):
+        return 1
+
+---
+
+(module
+  (class_definition
+    name: (identifier)
+    body: (block
+      (decorated_definition
+        (decorator (identifier))
+        (comment)
+        definition: (function_definition
+          name: (identifier)
+          parameters: (parameters (identifier))
+          body: (block (return_statement (integer))))))))
