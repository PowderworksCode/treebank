diff --git a/grammar.js b/grammar.js
index 3b1134c..7ec1c43 100644
--- a/grammar.js
+++ b/grammar.js
@@ -522,14 +522,18 @@ module.exports = grammar({
       $._dedent,
     ),
 
+    // CPython's `star_expressions`: an element of a bare tuple may be a
+    // starred expression. `*b,` already parsed only because a bare name is
+    // also a valid assignment-target pattern; `*[1],` had no path at all,
+    // since $.list_splat is not an $.expression.
     expression_list: $ => prec.right(seq(
-      $.expression,
+      choice($.expression, $.list_splat),
       choice(
         ',',
         seq(
           repeat1(seq(
             ',',
-            $.expression,
+            choice($.expression, $.list_splat),
           )),
           optional(','),
         ),
@@ -682,9 +686,12 @@ module.exports = grammar({
       field('value', $.expression),
     )),
 
+    // CPython's `star_target` takes a `target_with_star_atom`, which
+    // includes a bracketed or parenthesised target list: `*[], a = (1,)`
+    // and `*(), a = (1,)` are legal.
     list_splat_pattern: $ => seq(
       '*',
-      choice($.identifier, $.keyword_identifier, $.subscript, $.attribute),
+      choice($.identifier, $.keyword_identifier, $.subscript, $.attribute, $.list_pattern, $.tuple_pattern),
     ),
 
     dictionary_splat_pattern: $ => seq(
diff --git a/test/corpus/statements.txt b/test/corpus/statements.txt
index 3c73e01..23f8d19 100644
--- a/test/corpus/statements.txt
+++ b/test/corpus/statements.txt
@@ -1650,3 +1650,22 @@ class C:
           name: (identifier)
           parameters: (parameters (identifier))
           body: (block (return_statement (integer))))))))
+
+==================================
+Starred expressions in bare tuples and targets
+==================================
+
+a = *[1],
+*[], b = (1,)
+
+---
+
+(module
+  (expression_statement
+    (assignment
+      left: (identifier)
+      right: (expression_list (list_splat (list (integer))))))
+  (expression_statement
+    (assignment
+      left: (pattern_list (list_splat_pattern (list_pattern)) (identifier))
+      right: (tuple (integer)))))
