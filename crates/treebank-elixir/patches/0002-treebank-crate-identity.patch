diff --git a/Cargo.toml b/Cargo.toml
index c99ab5c..5d49b80 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -1,23 +1,30 @@
 [package]
-name = "tree-sitter-elixir"
-description = "Elixir grammar for the tree-sitter parsing library"
+name = "treebank-grammar-elixir"
+description = "Patched redistribution of elixir-lang/tree-sitter-elixir: upstream at a pinned commit plus the treebank patch series. Not an upstream release."
 version = "0.3.5"
 keywords = ["incremental", "parsing", "elixir"]
 categories = ["parsing", "text-editors"]
-repository = "https://github.com/elixir-lang/tree-sitter-elixir"
+repository = "https://github.com/PowderworksCode/treebank"
+homepage = "https://treebank.dev"
 edition = "2018"
 license = "Apache-2.0"
 
 build = "bindings/rust/build.rs"
 include = [
   "LICENSE",
+  "LOCAL-PATCHES.md",
+  "NOTICE",
   "bindings/rust/*",
   "grammar.js",
+  "ledger.json",
+  "patches/*",
   "queries/*",
   "src/*",
+  "tree-sitter.json",
 ]
 
 [lib]
+name = "tree_sitter_elixir"
 path = "bindings/rust/lib.rs"
 
 [dependencies]
