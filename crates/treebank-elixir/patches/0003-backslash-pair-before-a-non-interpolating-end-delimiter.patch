diff --git a/src/scanner.c b/src/scanner.c
index 9e744a1..e5bd5db 100644
--- a/src/scanner.c
+++ b/src/scanner.c
@@ -217,6 +217,14 @@ static bool scan_quoted_content(TSLexer *lexer, const QuotedContentInfo *info) {
         } else if (info->supports_interpol ||
                    lexer->lookahead == info->end_delimiter) {
           return has_content;
+        } else if (lexer->lookahead == '\\') {
+          // A backslash pair. In a non-interpolating string or sigil a
+          // backslash is literal content and only ever escapes the end
+          // delimiter, so `\\` is a completed pair and the second
+          // backslash cannot begin a new escape -- consuming it here is
+          // what stops `~S(\\)` being read as `\` followed by an escaped
+          // delimiter, which leaves the sigil unterminated.
+          advance(lexer);
         }
       } else if (lexer->lookahead == '\0') {
         // If we reached the end of the file, this means there is no
diff --git a/test/corpus/expression/sigil.txt b/test/corpus/expression/sigil.txt
index 16560bb..7f41e53 100644
--- a/test/corpus/expression/sigil.txt
+++ b/test/corpus/expression/sigil.txt
@@ -276,3 +276,24 @@ modifiers
     (sigil_name)
     (ERROR)
     (quoted_content)))
+
+================================================================================
+Backslash pair before the end delimiter
+================================================================================
+
+~S(\\)
+~S[\\]
+~S"\\"
+~S(\\\\)
+~S(a\\b)
+~s(\\)
+
+--------------------------------------------------------------------------------
+
+(source
+  (sigil (sigil_name) (quoted_content))
+  (sigil (sigil_name) (quoted_content))
+  (sigil (sigil_name) (quoted_content))
+  (sigil (sigil_name) (quoted_content))
+  (sigil (sigil_name) (quoted_content))
+  (sigil (sigil_name) (escape_sequence)))
