# rustish: do the three lowerings confer?

GENERATED by tools/confer.py from the corpus, antlr-results.md and winnow-results.md. tree-sitter's column is the corpus expectation, held by `tree-sitter test`.

3 cases: 2 agree, 0 where tree-sitter widened past the source and a backend rejects as the source does, 1 where a backend differs.

| case | tree-sitter | ANTLR | winnow | |
|---|---|---|---|---|
| a let is a statement with a pattern and a value; a block has statements and a tail | ✓ | ✓ | ✓ | agree |
| a fn item inside a block is a statement; a block is an expression; a return type | ✓ | ✓ | ✓ | agree |
| a block as a statement needs no semicolon; a comment is an extra | ✓ | ✗ | ✓ | differ |

## Where they differ

### a block as a statement needs no semicolon; a comment is an extra

```
expected (tree-sitter): (program (fn name: (id) body: (block (comment) (block (let pattern: (id) value: (exp_int (int)))))))
ANTLR:                  (program (fn name: (id) body: (block (block (let pattern: (id) value: (exp_int (int)))))))
```

