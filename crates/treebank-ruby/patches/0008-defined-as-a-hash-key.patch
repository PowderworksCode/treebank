diff --git a/grammar.js b/grammar.js
index 425a4f6..bd9716e 100644
--- a/grammar.js
+++ b/grammar.js
@@ -1280,6 +1280,11 @@ module.exports = grammar({
           alias($.constant, $.hash_key_symbol),
           alias($.identifier_suffix, $.hash_key_symbol),
           alias($.constant_suffix, $.hash_key_symbol),
+          // `defined?` is the one reserved word ending in `?`, so it is the
+          // one `identifier_suffix` cannot supply: at equal length the
+          // literal keyword token outranks the identifier regex. rubocop-ast
+          // keys a node-type map with it (`defined?: DefinedNode`).
+          alias('defined?', $.hash_key_symbol),
         )),
         token.immediate(':'),
         choice(
diff --git a/test/corpus/literals.txt b/test/corpus/literals.txt
index 45c8cf7..94e932c 100644
--- a/test/corpus/literals.txt
+++ b/test/corpus/literals.txt
@@ -2341,3 +2341,20 @@ c = ?\C-a
   (assignment left: (identifier) right: (character))
   (assignment left: (identifier) right: (character))
   (assignment left: (identifier) right: (character)))
+
+===========
+defined? as a hash key
+===========
+
+h = {defined?: A, def: B, if: C, foo?: D}
+
+---
+
+(program
+  (assignment
+    left: (identifier)
+    right: (hash
+      (pair key: (hash_key_symbol) value: (constant))
+      (pair key: (hash_key_symbol) value: (constant))
+      (pair key: (hash_key_symbol) value: (constant))
+      (pair key: (hash_key_symbol) value: (constant)))))
