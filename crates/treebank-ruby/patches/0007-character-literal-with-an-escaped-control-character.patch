diff --git a/grammar.js b/grammar.js
index b5c8550..425a4f6 100644
--- a/grammar.js
+++ b/grammar.js
@@ -1166,7 +1166,11 @@ module.exports = grammar({
 
     chained_string: $ => seq($.string, repeat1($.string)),
 
-    character: _ => /\?(\\\S(\{[0-9A-Fa-f]*\}|[0-9A-Fa-f]*|-\S([MC]-\S)?)?|\S)/,
+    // The character escaped by \C-, \c- or \M- may itself be an escape:
+    // `?\C-\]` is control-] and appears in ruby_parser's lexer tests. The old
+    // `-\S` consumed only the backslash and left the `]` behind, unbalancing
+    // whatever bracket was open around it.
+    character: _ => /\?(\\\S(\{[0-9A-Fa-f]*\}|[0-9A-Fa-f]*|-\\?\S([MC]-\\?\S)?)?|\S)/,
 
     interpolation: $ => choice(
       seq('#{', optional($._statements), '}'),
diff --git a/test/corpus/literals.txt b/test/corpus/literals.txt
index df0944e..45c8cf7 100644
--- a/test/corpus/literals.txt
+++ b/test/corpus/literals.txt
@@ -2326,3 +2326,18 @@ h = {:$: => [:$LOAD_PATH]}
       (pair
         key: (simple_symbol)
         value: (array (simple_symbol))))))
+
+===========
+character literal with an escaped control character
+===========
+
+a = ?\C-\]
+b = ?\M-\C-a
+c = ?\C-a
+
+---
+
+(program
+  (assignment left: (identifier) right: (character))
+  (assignment left: (identifier) right: (character))
+  (assignment left: (identifier) right: (character)))
