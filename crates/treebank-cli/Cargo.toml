[package]
name = "treebank-cli"
version = "0.1.0"
# Only the `treebank` crate is published. Everything else is a workspace
# member: the grammars ship as wasm packs and the CLI is not a library.
publish = false
edition = "2021"
license = "MIT"

[[bin]]
name = "treebank"
path = "src/main.rs"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
libloading = "0.8"
rayon = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.9"
sha2 = "0.10"
tree-sitter = "0.26"
treebank = { path = "../treebank" }
treebank-corpus = { path = "../treebank-corpus" }
treebank-lang = { path = "../treebank-lang" }
treebank-oracle = { path = "../treebank-oracle" }
treebank-preprocessing = { path = "../treebank-preprocessing" }
