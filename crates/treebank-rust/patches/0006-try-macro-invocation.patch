diff --git a/grammar.js b/grammar.js
index 2f20fae..2d1a43a 100644
--- a/grammar.js
+++ b/grammar.js
@@ -990,6 +990,9 @@ module.exports = grammar({
         // Macros can be named like primitive types (snapbox's `str!`): the
         // name is an identifier to rustc, same aliasing as in `_path`.
         alias(choice(...primitiveTypes), $.identifier),
+        // `try` is only a keyword in edition 2018+; `try!(...)` is valid
+        // 2015-edition code and unambiguous here (a try_block needs `{`).
+        alias('try', $.identifier),
         $._reserved_identifier,
       )),
       '!',
diff --git a/test/corpus/macros.txt b/test/corpus/macros.txt
index 6e82af5..d6be001 100644
--- a/test/corpus/macros.txt
+++ b/test/corpus/macros.txt
@@ -337,3 +337,26 @@ fn main() {
           macro: (identifier)
           (token_tree
             (integer_literal)))))))
+
+================================================================================
+try! macro invocation (2015 edition)
+================================================================================
+
+fn f() {
+    let v = try!(g());
+}
+
+--------------------------------------------------------------------------------
+
+(source_file
+  (function_item
+    name: (identifier)
+    parameters: (parameters)
+    body: (block
+      (let_declaration
+        pattern: (identifier)
+        value: (macro_invocation
+          macro: (identifier)
+          (token_tree
+            (identifier)
+            (token_tree)))))))
