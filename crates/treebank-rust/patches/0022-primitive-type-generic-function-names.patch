diff --git a/grammar.js b/grammar.js
index e3c8bd4..d256646 100644
--- a/grammar.js
+++ b/grammar.js
@@ -861,6 +861,9 @@ module.exports = grammar({
     generic_function: $ => prec(1, seq(
       field('function', choice(
         $.identifier,
+        // Functions can be named like primitive types (nom's `f32` parser):
+        // the name is an identifier to rustc, same aliasing as in `_path`.
+        alias(choice(...primitiveTypes), $.identifier),
         $.scoped_identifier,
         $.field_expression,
       )),
diff --git a/test/corpus/expressions.txt b/test/corpus/expressions.txt
index ecc7981..104d7b8 100644
--- a/test/corpus/expressions.txt
+++ b/test/corpus/expressions.txt
@@ -1487,3 +1487,39 @@ fn f() {
               (call_expression
                 (identifier)
                 (arguments)))))))))
+
+================================================================================
+Generic function named like a primitive type
+================================================================================
+
+fn f() {
+    let be_f32 = |s| f32::<_, (_, ErrorKind)>(Endianness::Big)(s);
+}
+
+--------------------------------------------------------------------------------
+
+(source_file
+  (function_item
+    (identifier)
+    (parameters)
+    (block
+      (let_declaration
+        (identifier)
+        (closure_expression
+          (closure_parameters
+            (identifier))
+          (call_expression
+            (call_expression
+              (generic_function
+                (identifier)
+                (type_arguments
+                  (type_identifier)
+                  (tuple_type
+                    (type_identifier)
+                    (type_identifier))))
+              (arguments
+                (scoped_identifier
+                  (identifier)
+                  (identifier))))
+            (arguments
+              (identifier))))))))
