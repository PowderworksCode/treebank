diff --git a/grammar.js b/grammar.js
index aa6bbc8..de7e891 100644
--- a/grammar.js
+++ b/grammar.js
@@ -873,6 +873,9 @@ module.exports = grammar({
         $._reserved_identifier,
         $.scoped_type_identifier,
       )),
+      // The turbofish is redundant in type position but accepted by rustc:
+      // `Vec::<u8>` is the same type as `Vec<u8>`.
+      optional('::'),
       field('type_arguments', $.type_arguments),
     )),
 
diff --git a/test/corpus/types.txt b/test/corpus/types.txt
index 9235504..f0ab7a8 100644
--- a/test/corpus/types.txt
+++ b/test/corpus/types.txt
@@ -411,3 +411,57 @@ type Weight = Bounded<-1.5, 1.5>;
         (negative_literal
           (float_literal))
         (float_literal)))))
+
+================================================================================
+Turbofish in type position
+================================================================================
+
+type Pairs = Punctuated::<Ident, Token>;
+
+fn f(x: &Gray_v09::<T>) -> Result::<Vec::<Tag>, Error> {
+    let _ = Result::<Vec::<Tag>, Error>::Ok(x);
+}
+
+--------------------------------------------------------------------------------
+
+(source_file
+  (type_item
+    (type_identifier)
+    (generic_type
+      (type_identifier)
+      (type_arguments
+        (type_identifier)
+        (type_identifier))))
+  (function_item
+    (identifier)
+    (parameters
+      (parameter
+        (identifier)
+        (reference_type
+          (generic_type
+            (type_identifier)
+            (type_arguments
+              (type_identifier))))))
+    (generic_type
+      (type_identifier)
+      (type_arguments
+        (generic_type
+          (type_identifier)
+          (type_arguments
+            (type_identifier)))
+        (type_identifier)))
+    (block
+      (let_declaration
+        (call_expression
+          (scoped_identifier
+            (generic_type
+              (type_identifier)
+              (type_arguments
+                (generic_type
+                  (type_identifier)
+                  (type_arguments
+                    (type_identifier)))
+                (type_identifier)))
+            (identifier))
+          (arguments
+            (identifier)))))))
