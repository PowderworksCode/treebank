diff --git a/grammar.js b/grammar.js
index 953440a..c3ad8c0 100644
--- a/grammar.js
+++ b/grammar.js
@@ -1127,7 +1127,10 @@ module.exports = grammar({
         [PREC.bitand, '&'],
         [PREC.bitor, '|'],
         [PREC.bitxor, '^'],
-        [PREC.comparative, choice('==', '!=', '<', '<=', '>', '>=')],
+        // `<=` needs the same token precedence as `type_arguments`' `<`, or
+        // the lexer prefers the higher-precedence `<` after a cast to a
+        // non-primitive type (`x as size_t <= y`) and splits the operator.
+        [PREC.comparative, choice('==', '!=', '<', token(prec(1, '<=')), '>', '>=')],
         [PREC.shift, choice('<<', '>>')],
         [PREC.additive, choice('+', '-')],
         [PREC.multiplicative, choice('*', '/', '%')],
diff --git a/test/corpus/expressions.txt b/test/corpus/expressions.txt
index 5117719..ecc7981 100644
--- a/test/corpus/expressions.txt
+++ b/test/corpus/expressions.txt
@@ -1452,3 +1452,38 @@ fn f() {
                   (string_literal
                     (string_content))))))
           (identifier))))))
+
+================================================================================
+Comparison against a cast to a non-primitive type
+================================================================================
+
+fn f() {
+    if a.b(c) as size_t <= length {
+        g();
+    }
+}
+
+--------------------------------------------------------------------------------
+
+(source_file
+  (function_item
+    (identifier)
+    (parameters)
+    (block
+      (expression_statement
+        (if_expression
+          (binary_expression
+            (type_cast_expression
+              (call_expression
+                (field_expression
+                  (identifier)
+                  (field_identifier))
+                (arguments
+                  (identifier)))
+              (type_identifier))
+            (identifier))
+          (block
+            (expression_statement
+              (call_expression
+                (identifier)
+                (arguments)))))))))
