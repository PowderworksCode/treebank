diff --git a/grammar.js b/grammar.js
index 68c6393..b28d330 100644
--- a/grammar.js
+++ b/grammar.js
@@ -532,6 +532,7 @@ module.exports = grammar({
         $.unit_type,
         $.array_type,
         $.higher_ranked_trait_bound,
+        $.macro_invocation,
         alias(choice(...primitiveTypes), $.primitive_type),
       )),
       field('bounds', $.trait_bounds),
diff --git a/test/corpus/declarations.txt b/test/corpus/declarations.txt
index b57756f..53a5a4a 100644
--- a/test/corpus/declarations.txt
+++ b/test/corpus/declarations.txt
@@ -2751,3 +2751,48 @@ async fn nest_fixture_with_default(
     (primitive_type)
     (block
       (identifier))))
+
+================================================================================
+Macro invocation on the left of a where predicate
+================================================================================
+
+fn f<T>()
+where
+    mac!(Self): Send,
+    mac!(T): Trait<Assoc = mac!(Self)>,
+{
+}
+
+--------------------------------------------------------------------------------
+
+(source_file
+  (function_item
+    (identifier)
+    (type_parameters
+      (type_parameter
+        (type_identifier)))
+    (parameters)
+    (where_clause
+      (where_predicate
+        (macro_invocation
+          (identifier)
+          (token_tree
+            (identifier)))
+        (trait_bounds
+          (type_identifier)))
+      (where_predicate
+        (macro_invocation
+          (identifier)
+          (token_tree
+            (identifier)))
+        (trait_bounds
+          (generic_type
+            (type_identifier)
+            (type_arguments
+              (type_binding
+                (type_identifier)
+                (macro_invocation
+                  (identifier)
+                  (token_tree
+                    (identifier)))))))))
+    (block)))
