diff --git a/grammar.js b/grammar.js
index ed5a690..68c6393 100644
--- a/grammar.js
+++ b/grammar.js
@@ -1208,8 +1208,8 @@ module.exports = grammar({
       '(',
       repeat($.attribute_item),
       seq($._expression, ','),
-      repeat(seq($._expression, ',')),
-      optional($._expression),
+      repeat(seq(repeat($.attribute_item), $._expression, ',')),
+      optional(seq(repeat($.attribute_item), $._expression)),
       ')',
     ),
 
diff --git a/test/corpus/expressions.txt b/test/corpus/expressions.txt
index 998d590..5117719 100644
--- a/test/corpus/expressions.txt
+++ b/test/corpus/expressions.txt
@@ -1408,3 +1408,47 @@ let a = &raw const b;
     (identifier)
     (reference_expression
       (identifier))))
+
+================================================================================
+Attributes on tuple expression elements
+================================================================================
+
+fn f() {
+    let t = (
+        channel,
+        #[cfg(feature = "retry")]
+        retry_policy,
+        #[cfg(not(feature = "retry"))]
+        None,
+    );
+}
+
+--------------------------------------------------------------------------------
+
+(source_file
+  (function_item
+    (identifier)
+    (parameters)
+    (block
+      (let_declaration
+        (identifier)
+        (tuple_expression
+          (identifier)
+          (attribute_item
+            (attribute
+              (identifier)
+              (token_tree
+                (identifier)
+                (string_literal
+                  (string_content)))))
+          (identifier)
+          (attribute_item
+            (attribute
+              (identifier)
+              (token_tree
+                (identifier)
+                (token_tree
+                  (identifier)
+                  (string_literal
+                    (string_content))))))
+          (identifier))))))
