diff --git a/grammar.js b/grammar.js
index c3ad8c0..e3c8bd4 100644
--- a/grammar.js
+++ b/grammar.js
@@ -1497,6 +1497,8 @@ module.exports = grammar({
       field('type', choice(
         $._type_identifier,
         $.scoped_type_identifier,
+        // Patterns spell generics with a turbofish: `Range::<T> { start, end }`.
+        alias($.generic_type_with_turbofish, $.generic_type),
       )),
       '{',
       sepBy(',', seq(
diff --git a/test/corpus/patterns.txt b/test/corpus/patterns.txt
index a28e4db..4b089fc 100644
--- a/test/corpus/patterns.txt
+++ b/test/corpus/patterns.txt
@@ -630,3 +630,30 @@ fn f(Tuple { 1: _int, .. }: Tuple<Droppable, i32>) {}
             (type_identifier)
             (primitive_type)))))
     (block)))
+
+================================================================================
+Turbofish generics in a struct pattern
+================================================================================
+
+fn f() {
+    let Range::<Idx> { start, end } = r;
+}
+
+--------------------------------------------------------------------------------
+
+(source_file
+  (function_item
+    (identifier)
+    (parameters)
+    (block
+      (let_declaration
+        (struct_pattern
+          (generic_type
+            (type_identifier)
+            (type_arguments
+              (type_identifier)))
+          (field_pattern
+            (shorthand_field_identifier))
+          (field_pattern
+            (shorthand_field_identifier)))
+        (identifier)))))
