diff --git a/grammar.js b/grammar.js
index de7e891..ed5a690 100644
--- a/grammar.js
+++ b/grammar.js
@@ -710,7 +710,7 @@ module.exports = grammar({
     parameters: $ => seq(
       '(',
       sepBy(',', seq(
-        optional($.attribute_item),
+        repeat($.attribute_item),
         choice(
           $.parameter,
           $.self_parameter,
diff --git a/test/corpus/declarations.txt b/test/corpus/declarations.txt
index 568bed7..b57756f 100644
--- a/test/corpus/declarations.txt
+++ b/test/corpus/declarations.txt
@@ -2716,3 +2716,38 @@ where
         bounds: (trait_bounds
           (type_identifier)
           (type_identifier))))))
+
+================================================================================
+Multiple attributes on a function parameter
+================================================================================
+
+async fn nest_fixture_with_default(
+    #[future]
+    #[default(async { 42 })]
+    fortytwo: u32,
+) -> u32 {
+    fortytwo
+}
+
+--------------------------------------------------------------------------------
+
+(source_file
+  (function_item
+    (function_modifiers)
+    (identifier)
+    (parameters
+      (attribute_item
+        (attribute
+          (identifier)))
+      (attribute_item
+        (attribute
+          (identifier)
+          (token_tree
+            (token_tree
+              (integer_literal)))))
+      (parameter
+        (identifier)
+        (primitive_type)))
+    (primitive_type)
+    (block
+      (identifier))))
