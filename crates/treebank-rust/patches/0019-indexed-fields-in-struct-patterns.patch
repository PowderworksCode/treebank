diff --git a/grammar.js b/grammar.js
index b28d330..953440a 100644
--- a/grammar.js
+++ b/grammar.js
@@ -1510,7 +1510,9 @@ module.exports = grammar({
       choice(
         field('name', alias($.identifier, $.shorthand_field_identifier)),
         seq(
-          field('name', $._field_identifier),
+          // A tuple struct's fields are named by index: `Tuple { 1: x, .. }`,
+          // same idiom as `field_expression`.
+          field('name', choice($._field_identifier, $.integer_literal)),
           ':',
           field('pattern', $._pattern),
         ),
diff --git a/test/corpus/patterns.txt b/test/corpus/patterns.txt
index 5039d2b..a28e4db 100644
--- a/test/corpus/patterns.txt
+++ b/test/corpus/patterns.txt
@@ -604,3 +604,29 @@ fn f(t: T) {
                     pattern: (identifier))
                   (remaining_field_pattern)))
               value: (block))))))))
+
+================================================================================
+Tuple struct fields named by index in a struct pattern
+================================================================================
+
+fn f(Tuple { 1: _int, .. }: Tuple<Droppable, i32>) {}
+
+--------------------------------------------------------------------------------
+
+(source_file
+  (function_item
+    (identifier)
+    (parameters
+      (parameter
+        (struct_pattern
+          (type_identifier)
+          (field_pattern
+            (integer_literal)
+            (identifier))
+          (remaining_field_pattern))
+        (generic_type
+          (type_identifier)
+          (type_arguments
+            (type_identifier)
+            (primitive_type)))))
+    (block)))
