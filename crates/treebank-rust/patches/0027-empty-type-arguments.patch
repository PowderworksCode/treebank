diff --git a/grammar.js b/grammar.js
index 6fdbf81..601d536 100644
--- a/grammar.js
+++ b/grammar.js
@@ -919,18 +919,21 @@ module.exports = grammar({
 
     type_arguments: $ => seq(
       token(prec(1, '<')),
-      sepBy1(',', seq(
-        choice(
-          $._type,
-          $.type_binding,
-          $.lifetime,
-          $._literal,
-          $.negative_literal,
-          $.block,
-        ),
-        optional($.trait_bounds),
+      // An empty argument list (`Thing<>`) is accepted by rustc.
+      optional(seq(
+        sepBy1(',', seq(
+          choice(
+            $._type,
+            $.type_binding,
+            $.lifetime,
+            $._literal,
+            $.negative_literal,
+            $.block,
+          ),
+          optional($.trait_bounds),
+        )),
+        optional(','),
       )),
-      optional(','),
       '>',
     ),
 
diff --git a/test/corpus/types.txt b/test/corpus/types.txt
index f0ab7a8..37bf3dd 100644
--- a/test/corpus/types.txt
+++ b/test/corpus/types.txt
@@ -465,3 +465,29 @@ fn f(x: &Gray_v09::<T>) -> Result::<Vec::<Tag>, Error> {
             (identifier))
           (arguments
             (identifier)))))))
+
+================================================================================
+Empty type arguments
+================================================================================
+
+extern "C++" {
+    fn zero(self: &Thing<>);
+}
+
+--------------------------------------------------------------------------------
+
+(source_file
+  (foreign_mod_item
+    (extern_modifier
+      (string_literal
+        (string_content)))
+    (declaration_list
+      (function_signature_item
+        (identifier)
+        (parameters
+          (parameter
+            (self)
+            (reference_type
+              (generic_type
+                (type_identifier)
+                (type_arguments)))))))))
