diff --git a/grammar.js b/grammar.js
index d256646..2cf6354 100644
--- a/grammar.js
+++ b/grammar.js
@@ -1566,7 +1566,8 @@ module.exports = grammar({
     ),
 
     captured_pattern: $ => seq(
-      $.identifier,
+      // Contextual keywords bind like any other name: `raw @ (A | B)`.
+      choice($.identifier, $._reserved_identifier),
       '@',
       $._pattern,
     ),
diff --git a/test/corpus/patterns.txt b/test/corpus/patterns.txt
index 4b089fc..5950572 100644
--- a/test/corpus/patterns.txt
+++ b/test/corpus/patterns.txt
@@ -657,3 +657,43 @@ fn f() {
           (field_pattern
             (shorthand_field_identifier)))
         (identifier)))))
+
+================================================================================
+Contextual keyword as a capture name
+================================================================================
+
+fn f(r: T) {
+    match r {
+        raw @ (U8 | U16 | U32) => {}
+        _ => {}
+    }
+}
+
+--------------------------------------------------------------------------------
+
+(source_file
+  (function_item
+    (identifier)
+    (parameters
+      (parameter
+        (identifier)
+        (type_identifier)))
+    (block
+      (expression_statement
+        (match_expression
+          (identifier)
+          (match_block
+            (match_arm
+              (match_pattern
+                (captured_pattern
+                  (identifier)
+                  (tuple_pattern
+                    (or_pattern
+                      (or_pattern
+                        (identifier)
+                        (identifier))
+                      (identifier)))))
+              (block))
+            (match_arm
+              (match_pattern)
+              (block))))))))
