diff --git a/grammar.js b/grammar.js
index 37a1e2c..5d94cce 100644
--- a/grammar.js
+++ b/grammar.js
@@ -992,6 +992,7 @@ module.exports = grammar({
 
     _expansion_max_length: $ => seq(
       field('operator', ':'),
+      optional($.simple_expansion),
       optional(choice(
         $._simple_variable_name,
         $.number,
diff --git a/test/corpus/literals.txt b/test/corpus/literals.txt
index 314b116..da41cf0 100644
--- a/test/corpus/literals.txt
+++ b/test/corpus/literals.txt
@@ -1748,3 +1748,63 @@ echo {0..2}{1..2}
       (brace_expression
         (number)
         (number)))))
+
+================================================================================
+Substring expansion with a variable offset
+================================================================================
+
+echo "${var:$offset:1}"
+echo "${var:$offset}"
+echo "${arr[@]:$keep_count}"
+echo "${var:$i:$j}"
+echo "${var:1:$n}"
+
+--------------------------------------------------------------------------------
+
+(program
+  (command
+    (command_name
+      (word))
+    (string
+      (expansion
+        (variable_name)
+        (simple_expansion
+          (variable_name))
+        (number))))
+  (command
+    (command_name
+      (word))
+    (string
+      (expansion
+        (variable_name)
+        (simple_expansion
+          (variable_name)))))
+  (command
+    (command_name
+      (word))
+    (string
+      (expansion
+        (subscript
+          (variable_name)
+          (word))
+        (simple_expansion
+          (variable_name)))))
+  (command
+    (command_name
+      (word))
+    (string
+      (expansion
+        (variable_name)
+        (simple_expansion
+          (variable_name))
+        (simple_expansion
+          (variable_name)))))
+  (command
+    (command_name
+      (word))
+    (string
+      (expansion
+        (variable_name)
+        (number)
+        (simple_expansion
+          (variable_name))))))
