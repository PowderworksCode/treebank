diff --git a/src/scanner.c b/src/scanner.c
index 7dc82c0..43cb60e 100644
--- a/src/scanner.c
+++ b/src/scanner.c
@@ -747,6 +747,7 @@ regex:
                 bool advanced_once;
                 bool found_non_alnumdollarunderdash;
                 bool last_was_escape;
+                bool last_was_dollar;
                 bool in_single_quote;
                 uint32_t paren_depth;
                 uint32_t bracket_depth;
@@ -763,7 +764,7 @@ regex:
 
             lexer->mark_end(lexer);
 
-            State state = {false, false, false, false, false, 0, 0, 0};
+            State state = {false, false, false, false, false, false, 0, 0, 0};
             while (!state.done) {
                 if (state.in_single_quote) {
                     if (lexer->lookahead == '\'') {
@@ -787,7 +788,10 @@ regex:
                         state.last_was_escape = false;
                         break;
                     case '{':
-                        if (!state.last_was_escape) {
+                        // Inside a bracket expression a bare `{` is an
+                        // ordinary character (`${v%%[={ ]*}`), but `${` is
+                        // still a nested expansion (`${n%[ "${nl}"]*}`).
+                        if (!state.last_was_escape && (state.bracket_depth == 0 || state.last_was_dollar)) {
                             state.brace_depth++;
                         }
                         state.last_was_escape = false;
@@ -826,6 +830,7 @@ regex:
                 }
 
                 if (!state.done) {
+                    state.last_was_dollar = lexer->lookahead == '$';
                     if (valid_symbols[REGEX]) {
                         bool was_space = !state.in_single_quote && iswspace(lexer->lookahead);
                         advance(lexer);
diff --git a/test/corpus/literals.txt b/test/corpus/literals.txt
index 0243d2b..49bde21 100644
--- a/test/corpus/literals.txt
+++ b/test/corpus/literals.txt
@@ -2096,3 +2096,21 @@ address=${address: ${#address} < 8 ? 0 : -8}
           (number))
         consequence: (number)
         alternative: (number)))))
+
+================================================================================
+Brace inside a bracket expression in a pattern
+================================================================================
+
+CONF["${v%%[={ ]*}"]=1
+
+--------------------------------------------------------------------------------
+
+(program
+  (variable_assignment
+    name: (subscript
+      name: (variable_name)
+      index: (string
+        (expansion
+          (variable_name)
+          (regex))))
+    value: (number)))
