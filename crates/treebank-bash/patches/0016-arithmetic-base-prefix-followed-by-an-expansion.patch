diff --git a/grammar.js b/grammar.js
index 9413eee..d69c3c5 100644
--- a/grammar.js
+++ b/grammar.js
@@ -951,7 +951,9 @@ module.exports = grammar({
     number: $ => choice(
       /-?(0x)?[0-9]+(#[0-9A-Za-z@_]+)?/,
       // the base can be an expansion or command substitution
-      seq(/-?(0x)?[0-9]+#/, choice($.expansion, $.command_substitution)),
+      seq(/-?(0x)?[0-9]+#/, choice($.expansion, $.command_substitution, $.simple_expansion)),
+      // ...and so can the digits after a `0x` prefix: `$((0x$(setpci ...)))`
+      seq(/-?0[xX]/, choice($.expansion, $.command_substitution, $.simple_expansion)),
     ),
 
     simple_expansion: $ => seq(
diff --git a/test/corpus/literals.txt b/test/corpus/literals.txt
index 89893ef..6dc642a 100644
--- a/test/corpus/literals.txt
+++ b/test/corpus/literals.txt
@@ -1824,8 +1824,7 @@ plain=$((0x12 + a * 2))
   (variable_assignment
     (variable_name)
     (arithmetic_expansion
-      (concatenation
-        (variable_name)
+      (number
         (command_substitution
           (command
             (command_name
@@ -1834,8 +1833,7 @@ plain=$((0x12 + a * 2))
   (variable_assignment
     (variable_name)
     (arithmetic_expansion
-      (concatenation
-        (variable_name)
+      (number
         (expansion
           (variable_name)))))
   (variable_assignment
@@ -1950,3 +1948,28 @@ HOST=$(f "${HOST%%\]}")
             (variable_name)
             (regex)
             (regex)))))))
+
+================================================================================
+Arithmetic base prefix followed by an expansion
+================================================================================
+
+x=$((0x$(a)))
+y=$((16#$P))
+
+--------------------------------------------------------------------------------
+
+(program
+  (variable_assignment
+    name: (variable_name)
+    value: (arithmetic_expansion
+      (number
+        (command_substitution
+          (command
+            name: (command_name
+              (word)))))))
+  (variable_assignment
+    name: (variable_name)
+    value: (arithmetic_expansion
+      (number
+        (simple_expansion
+          (variable_name))))))
