diff --git a/grammar.js b/grammar.js
index d36d8e5..0b6a553 100644
--- a/grammar.js
+++ b/grammar.js
@@ -613,6 +613,9 @@ module.exports = grammar({
         ),
         $._heredoc_expression,
         $._heredoc_command,
+        // `cat <<HELP; exit` — a second statement on the same line, before
+        // the body that starts on the next one
+        seq(';', optional($._statement)),
       )),
       /\n/,
       choice($._heredoc_body, $._simple_heredoc_body),
diff --git a/src/scanner.c b/src/scanner.c
index ba8cd24..f31082e 100644
--- a/src/scanner.c
+++ b/src/scanner.c
@@ -172,7 +172,12 @@ static bool advance_word(TSLexer *lexer, String *unquoted_word) {
 
     while (lexer->lookahead &&
            !(quote ? lexer->lookahead == quote || lexer->lookahead == '\r' || lexer->lookahead == '\n'
-                   : iswspace(lexer->lookahead))) {
+                   : iswspace(lexer->lookahead) ||
+                         // an unquoted heredoc delimiter is a WORD, so it
+                         // ends at a metacharacter too: `cat <<HELP; exit`
+                         lexer->lookahead == ';' || lexer->lookahead == '&' || lexer->lookahead == '|' ||
+                         lexer->lookahead == '<' || lexer->lookahead == '>' || lexer->lookahead == '(' ||
+                         lexer->lookahead == ')')) {
         if (lexer->lookahead == '\\') {
             advance(lexer);
             if (!lexer->lookahead) {
diff --git a/test/corpus/statements.txt b/test/corpus/statements.txt
index 2b6d017..c186ef9 100644
--- a/test/corpus/statements.txt
+++ b/test/corpus/statements.txt
@@ -2134,3 +2134,39 @@ Test bracket as an ordinary command in a pipeline
       argument: (word)
       argument: (word)
       argument: (word))))
+
+================================================================================
+Heredoc delimiter followed by a metacharacter
+================================================================================
+
+cat <<HELP; exit
+hi
+HELP
+cat <<STOP>out
+x
+STOP
+
+--------------------------------------------------------------------------------
+
+(program
+  (redirected_statement
+    body: (command
+      name: (command_name
+        (word)))
+    redirect: (heredoc_redirect
+      (heredoc_start)
+      (command
+        name: (command_name
+          (word)))
+      (heredoc_body)
+      (heredoc_end)))
+  (redirected_statement
+    body: (command
+      name: (command_name
+        (word)))
+    redirect: (heredoc_redirect
+      (heredoc_start)
+      redirect: (file_redirect
+        destination: (word))
+      (heredoc_body)
+      (heredoc_end))))
