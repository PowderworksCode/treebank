diff --git a/grammar.js b/grammar.js
index 0b6a553..055939b 100644
--- a/grammar.js
+++ b/grammar.js
@@ -142,10 +142,21 @@ module.exports = grammar({
       // get this — `f() { ( x ) >/dev/null }` is a syntax error in bash too.
       seq(
         repeat(seq($._statement, $._terminator)),
-        $._compound_command,
+        choice(
+          $._compound_command,
+          // ...and the same when the compound is the right-hand side of an
+          // && / || list: `{ enabled x && { require y; } }`
+          alias($._unterminated_list, $.list),
+        ),
       ),
     ),
 
+    _unterminated_list: $ => prec.left(-1, seq(
+      $._statement,
+      choice('&&', '||'),
+      choice($._compound_command, alias($._unterminated_list, $.list)),
+    )),
+
     _compound_command: $ => choice(
       $.subshell,
       $.compound_statement,
diff --git a/test/corpus/statements.txt b/test/corpus/statements.txt
index ed3687f..cea08c4 100644
--- a/test/corpus/statements.txt
+++ b/test/corpus/statements.txt
@@ -2192,3 +2192,35 @@ esac
         value: (string
           (expansion
             (variable_name)))))))
+
+================================================================================
+Trailing list ending in a compound command
+================================================================================
+
+enabled x && { require y && { test z || die "e"; } }
+
+--------------------------------------------------------------------------------
+
+(program
+  (list
+    (command
+      name: (command_name
+        (word))
+      argument: (word))
+    (compound_statement
+      (list
+        (command
+          name: (command_name
+            (word))
+          argument: (word))
+        (compound_statement
+          (list
+            (command
+              name: (command_name
+                (word))
+              argument: (word))
+            (command
+              name: (command_name
+                (word))
+              argument: (string
+                (string_content)))))))))
