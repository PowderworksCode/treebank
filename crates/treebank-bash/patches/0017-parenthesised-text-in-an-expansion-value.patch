diff --git a/grammar.js b/grammar.js
index d69c3c5..d36d8e5 100644
--- a/grammar.js
+++ b/grammar.js
@@ -1170,6 +1170,9 @@ module.exports = grammar({
           $.ansi_c_string,
           $.command_substitution,
           alias($._expansion_word, $.word),
+          // a bare `)` inside `${...}` is text, as `_expansion_regex`
+          // already assumes for the pattern operands
+          alias(')', $.word),
           $.array,
           $.process_substitution,
         ),
diff --git a/src/scanner.c b/src/scanner.c
index 65a5339..7c45ceb 100644
--- a/src/scanner.c
+++ b/src/scanner.c
@@ -352,7 +352,11 @@ static bool scan(Scanner *scanner, TSLexer *lexer, const bool *valid_symbols) {
     bool expansion_word_advanced = false;
     if (valid_symbols[CONCAT] && !in_error_recovery(valid_symbols)) {
         if (!(lexer->lookahead == 0 || iswspace(lexer->lookahead) || lexer->lookahead == '>' ||
-              lexer->lookahead == '<' || lexer->lookahead == ')' || lexer->lookahead == '(' ||
+              lexer->lookahead == '<' ||
+              // inside `${...}` a `)` is ordinary text, so a concatenation
+              // may continue across it: `${A:+ (git:$B)}`
+              (lexer->lookahead == ')' && !valid_symbols[CLOSING_BRACE]) ||
+              lexer->lookahead == '(' ||
               lexer->lookahead == ';' || lexer->lookahead == '&' || lexer->lookahead == '|' ||
               (lexer->lookahead == '}' && valid_symbols[CLOSING_BRACE]) ||
               (lexer->lookahead == ']' && valid_symbols[CLOSING_BRACKET]))) {
diff --git a/test/corpus/literals.txt b/test/corpus/literals.txt
index 6dc642a..7c7d5ce 100644
--- a/test/corpus/literals.txt
+++ b/test/corpus/literals.txt
@@ -1973,3 +1973,26 @@ y=$((16#$P))
       (number
         (simple_expansion
           (variable_name))))))
+
+================================================================================
+Parenthesised text in an expansion default value
+================================================================================
+
+echo "$1${2:+ (from $2)}"
+
+--------------------------------------------------------------------------------
+
+(program
+  (command
+    name: (command_name
+      (word))
+    argument: (string
+      (simple_expansion
+        (variable_name))
+      (expansion
+        (variable_name)
+        (concatenation
+          (word)
+          (simple_expansion
+            (variable_name))
+          (word))))))
