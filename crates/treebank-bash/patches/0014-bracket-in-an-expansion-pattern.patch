diff --git a/grammar.js b/grammar.js
index ea1cfe4..9bfc57f 100644
--- a/grammar.js
+++ b/grammar.js
@@ -1037,6 +1037,10 @@ module.exports = grammar({
       repeat(choice(
         $.regex,
         alias(')', $.regex),
+        // the regex scanner stops at a `]` so that `[[ $x =~ a ]]` ends at
+        // the `]]`; inside `${var%%...}` a `]` is an ordinary pattern
+        // character, so pick it up the same way `)` already is
+        alias(']', $.regex),
         $.string,
         $.raw_string,
         alias(/\s+/, $.regex),
diff --git a/test/corpus/literals.txt b/test/corpus/literals.txt
index eb99977..89893ef 100644
--- a/test/corpus/literals.txt
+++ b/test/corpus/literals.txt
@@ -1922,3 +1922,31 @@ z="${SHLIB+PROVIDE (}"
         (concatenation
           (word)
           (word))))))
+
+================================================================================
+Bracket in an expansion pattern
+================================================================================
+
+x=${SPARE%%]*}
+HOST=$(f "${HOST%%\]}")
+
+--------------------------------------------------------------------------------
+
+(program
+  (variable_assignment
+    name: (variable_name)
+    value: (expansion
+      (variable_name)
+      (regex)
+      (regex)))
+  (variable_assignment
+    name: (variable_name)
+    value: (command_substitution
+      (command
+        name: (command_name
+          (word))
+        argument: (string
+          (expansion
+            (variable_name)
+            (regex)
+            (regex)))))))
