diff --git a/grammar.js b/grammar.js
index 79a136c..ab60332 100644
--- a/grammar.js
+++ b/grammar.js
@@ -165,7 +165,13 @@ module.exports = grammar({
       $.for_statement,
       $.c_style_for_statement,
       $.case_statement,
-      $.test_command,
+      // `[[ … ]]` only. bash lets a COMPOUND command end a list without a
+      // terminator, and `[[ ]]` is one — `{ [[ -f x ]] }` is valid. `[ ]` is
+      // not: `[` is an ordinary builtin, so `{ [ -f x ] }` is a syntax error,
+      // exactly like `{ echo hi }`. Naming the whole of `test_command` here
+      // admitted both, and made the grammar accept gimp's
+      // `&& { [ a ] || [ b ] }` — a real syntax error in a real script.
+      alias($._double_bracket_test, $.test_command),
       $.function_definition,
     ),
 
@@ -493,18 +499,21 @@ module.exports = grammar({
       ),
     ),
 
-    test_command: $ => seq(
+    test_command: $ => choice(
+      seq('[', optional(choice($._expression, $.redirected_statement)), ']'),
+      $._double_bracket_test,
+    ),
+
+    // Split out so that `_compound_command` can name the `[[ ]]` form
+    // WITHOUT the `[ ]` one. Aliased back to `test_command` wherever it is
+    // used, so the tree shape is unchanged.
+    _double_bracket_test: $ => seq(
+      '[[',
       choice(
-        seq('[', optional(choice($._expression, $.redirected_statement)), ']'),
-        seq(
-          '[[',
-          choice(
-            $._expression,
-            alias($._test_command_binary_expression, $.binary_expression),
-          ),
-          ']]',
-        ),
+        $._expression,
+        alias($._test_command_binary_expression, $.binary_expression),
       ),
+      ']]',
     ),
 
     _test_command_binary_expression: $ => prec(PREC.ASSIGN,
diff --git a/test/corpus/statements.txt b/test/corpus/statements.txt
index da02ce1..d21870e 100644
--- a/test/corpus/statements.txt
+++ b/test/corpus/statements.txt
@@ -2248,3 +2248,42 @@ Double equals as a command argument inside a compound
           (word))
         argument: (word)
         argument: (word)))))
+
+================================================================================
+Only a compound command may end a list without a terminator
+================================================================================
+
+{ ( echo hi ) }
+{ if x; then :; fi }
+{ [[ -f x ]] }
+{ a && [[ b ]] }
+
+--------------------------------------------------------------------------------
+
+(program
+  (compound_statement
+    (subshell
+      (command
+        (command_name
+          (word))
+        (word))))
+  (compound_statement
+    (if_statement
+      (command
+        (command_name
+          (word)))
+      (command
+        (command_name
+          (word)))))
+  (compound_statement
+    (test_command
+      (unary_expression
+        (test_operator)
+        (word))))
+  (compound_statement
+    (list
+      (command
+        (command_name
+          (word)))
+      (test_command
+        (word)))))
