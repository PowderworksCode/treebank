diff --git a/src/scanner.c b/src/scanner.c
index cb7edd1..7dc82c0 100644
--- a/src/scanner.c
+++ b/src/scanner.c
@@ -1091,7 +1091,11 @@ extglob_pattern:
                             state.saw_non_alphadot = true;
                         }
                         advance(lexer);
-                        if (iswspace(lexer->lookahead) || lexer->lookahead == '"') {
+                        // An escaped bracket is pattern text, not a
+                        // delimiter: `*\]:*` must not end at its `]`.
+                        if (iswspace(lexer->lookahead) || lexer->lookahead == '"' || lexer->lookahead == ']' ||
+                            lexer->lookahead == '[' || lexer->lookahead == ')' || lexer->lookahead == '(' ||
+                            lexer->lookahead == '}' || lexer->lookahead == '{') {
                             advance(lexer);
                         }
                     } else {
diff --git a/test/corpus/statements.txt b/test/corpus/statements.txt
index c186ef9..ed3687f 100644
--- a/test/corpus/statements.txt
+++ b/test/corpus/statements.txt
@@ -2170,3 +2170,25 @@ STOP
         destination: (word))
       (heredoc_body)
       (heredoc_end))))
+
+================================================================================
+Case pattern with an escaped bracket
+================================================================================
+
+case $1 in
+  *\]:*) real_server="${1}" ;;
+esac
+
+--------------------------------------------------------------------------------
+
+(program
+  (case_statement
+    value: (simple_expansion
+      (variable_name))
+    (case_item
+      value: (extglob_pattern)
+      (variable_assignment
+        name: (variable_name)
+        value: (string
+          (expansion
+            (variable_name)))))))
