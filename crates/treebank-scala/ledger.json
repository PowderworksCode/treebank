{
  "grammar": "scala",
  "upstream": {
    "git_url": "https://github.com/tree-sitter/tree-sitter-scala",
    "sha": "b931fcc338390925eb893d70ad070033f5856ccf",
    "version": "0.26.2"
  },
  "upstream_note": "The grammar's official home in the tree-sitter org, pushed the day this was vendored: 196 stars, not archived, v0.26.2 released 2026-08-08. Per ROADMAP.md's host analysis, tree-sitter/ is the only host with zero grammars over a year stale, so no fork evaluation was needed here.",
  "generate_cli": "0.25.10",
  "generate_cli_note": "same pin as the other grammars: tree-sitter-cli 0.26.x ships broken Unicode identifier tables (see treebank-rust ledger). Upstream develops against ^0.26.8, so this pin is a real downgrade rather than a formality \u2014 verified before vendoring: generate succeeds at 0.25.10 and all 272 of upstream's own corpus tests plus its highlight and tag assertions pass on the parser it produces.",
  "generate_dirs": [
    "."
  ],
  "generate_deps": null,
  "oracle": {
    "tool": "tools/scala-oracle/Check.java (scalameta Parse.parseSource), run with the JDK single-file source launcher over the jars pinned in tools/scala-oracle/jars.lock",
    "version": "scalameta 4.17.3 on OpenJDK 21.0.11",
    "dialect": "per file, from the Maven coordinate the file was published under",
    "flags": [
      "_3 -> Scala3",
      "_2.13 -> Scala213",
      "_2.12 -> Scala212",
      "_2.11 -> Scala211"
    ],
    "authority": "reference",
    "authority_note": "Scala HAS an implementation everyone appeals to \u2014 scalac \u2014 so a gap number here is a measurement rather than a measurement-relative-to-us, and `position` would overstate the uncertainty. But scalameta is not scalac, it is the parser Scala's own tooling appeals to in scalac's place, so the two can disagree and a disagreement is a bug in scalameta rather than a matter of opinion. One is known and recorded under `dialect_is_the_whole_problem.known_permissiveness`; it was found by the negative battery, not by the corpus. Stated explicitly rather than left to the field's default, because the default is a claim.",
    "stage": "parse",
    "stage_note": "Parse.parseSource() builds a Source tree and stops: no typing, no symbol resolution, no macro expansion. The stage is worth recording even though Scala's stages do not disagree the way yaml's parse-vs-load did \u2014 an unresolved import or a missing classpath is not an error here, which is exactly what makes a file judgeable on its own.",
    "note": "scalameta's parser, the one Metals and scalafmt are built on. It parses without typing, so unresolved imports and a missing classpath are not errors and each file is judged on its own, exactly like JavacTask.parse() for Java. `Parsed.Error` is the verdict `invalid`; anything THROWN out of the parser is an oracle failure and exits non-zero rather than becoming a verdict.",
    "smoke": {
      "requires": [
        "java"
      ],
      "build": "tools/scala-oracle/fetch-jars.sh",
      "valid": "tools/scala-oracle/fixtures/com.example__smoke_3-1.0.0/Valid.scala",
      "invalid": "tools/scala-oracle/fixtures/com.example__smoke_3-1.0.0/Invalid.scala",
      "note": "Dedicated fixtures rather than the consumer-test pair, for a reason particular to this language: the smoke test hands validate() REPO-relative paths, and a Scala file's dialect comes from the Maven coordinate in its path. tools/consumer-test/fixtures/patched.scala has no coordinate, so the oracle correctly refuses to give it a verdict at all \u2014 and patched.scala is deliberately valid under NO single dialect anyway, since it pins the grammar's Scala 2 + Scala 3 union. These two live under a directory named for a coordinate instead, which is how any file outside the corpus declares its dialect. Valid.scala is Scala 3 ONLY, so the smoke test also fails if dialect routing breaks and sends it to a Scala 2 dialect.",
      "build_note": "fetch-jars.sh verifies the sha256 of all nine pinned jars on every run, so a truncated or republished jar is caught here rather than becoming a verdict."
    },
    "dialect_is_the_whole_problem": {
      "claim": "Scala 2 and Scala 3 are different languages sharing the .scala extension, and scalameta requires the dialect per file. Nothing in a file's path settles it \u2014 but the Maven coordinate does, and the corpus directory carries it: cats-core_3 is Scala 3, spark-core_2.11 is Scala 2.11. So the dialect is declared per package by crates/treebank-cli/src/lang/scala.rs and passed to the oracle per file. The oracle never picks a dialect and never falls back to another one.",
      "measured_2026_08_13": {
        "corpus": "3,508 files from 32 Maven Central sources jars, all of them library code that compiles",
        "declared_per_package": "0 of 3508 valid files called invalid",
        "every_file_as_Scala213": "61 of 3508 (1.7%) called invalid",
        "every_file_as_Scala212": "62 of 3508 (1.8%) called invalid",
        "every_file_as_Scala211": "226 of 3508 (6.4%) called invalid",
        "every_file_as_Scala3": "301 of 3508 (8.6%) called invalid",
        "both_directions_are_real": "301 files parse under Scala213 but not Scala3 (procedure syntax, symbol literals, do/while \u2014 Flink and Akka 2.x); 61 parse under Scala3 but not Scala213 (given, inline, using \u2014 tapir, upickle, os-lib)."
      },
      "why_not_try_both": "Trying every dialect and keeping any success makes 100% of files valid by construction, which would drive gap_files to zero and report a flawless grammar. The measurement above is the reason it is also unnecessary: the declared dialect misrouted nothing, so the union would buy no coverage and cost the entire meaning of `invalid`.",
      "known_permissiveness": "scalameta's Scala213 dialect accepts `def f(using ) = 1`, which scalac 2.13 rejects (it reads `using` as a clause in every dialect). Found by the negative battery, not by the corpus. Scala3 rejects it. No corpus file depends on it.",
      "not_yet_needed": "scalameta also ships Scala213Source3 and Scala212Source3 for 2.x code compiled with -Xsource:3. A cross-built artifact could need one; none in this corpus did, so the four plain dialects are what is declared. If a sweep ever shows false rejects clustered in a -Xsource:3 project, that is the evidence to add them."
    },
    "cost_measured_2026_08_13": {
      "shape": "one long-lived JVM, serial, batch of paths on stdin",
      "s_per_1000": "4.39-4.64 when 1000 files is the whole batch; 2.74-2.93 over 3508; 1.12-1.40 marginal once the JIT is warm. JVM start alone is 0.23 s, and the single-file source launcher adds ~0.6 s of compile per invocation.",
      "batch_size_matters": "18.2 s/1000 for the first 100 files \u2014 this oracle must be called in batches, not per file.",
      "load_caveat": "measured with load average 5-20 on 16 shared cores, so these are upper bounds.",
      "roadmap_comparison": "ROADMAP.md entry #15 quotes no cost for scala. Against its measured peers (python 1.23, lua 1.7, go 1.94, bash 2.4 forked, php 18.3 serial, c++ 1068) this is firmly in the cheap tier; cost is not this language's problem, dialect is."
    },
    "why_it_does_not_parallelize": {
      "finding": "scala.meta.internal.tokenizers.PlatformTokenizerCache.megaCache is a ConcurrentHashMap of Dialect -> *non-concurrent* mutable.Map, so two threads parsing under the same dialect race and throw ConcurrentModificationException.",
      "measured": "at 4 / 8 / 16 threads, 1 / 8 / 15 valid files flipped to invalid, a different set each run. A false `invalid` records a real grammar gap as corpus noise, so a threaded oracle would quietly agree with us \u2014 the failure GRAMMARS.md names.",
      "process_level_too": "separate JVMs are safe but barely pay: 1/4/8/16 processes over 3508 files ran 11.0 / 8.9 / 12.9 / 18.7 s wall, best case ~1.24x, because per-process JIT warmup is a large fraction of the job. The `-P16` lever that PHP's entry generalizes to 'lua, fish, awk and a dozen more' does not reach a JVM-resident oracle. Hence: serial, one JVM, one worker thread (which exists only for its 512 MB stack).",
      "cache_is_not_a_leak": "megaCache clears per parse; heap stayed at 6 MB across 10,524 parses."
    }
  },
  "corpus": {
    "source": "Maven Central, ranked by dependent repositories (ecosyste.ms index of repo1.maven.org), fetched 2026-08-13; each coordinate's -sources.jar at the version maven-metadata.xml calls <release>; .scala files",
    "files": 17665,
    "packages": 394,
    "projects": 263,
    "fetch_limit": 394,
    "fetch_limit_note": "The size the numbers below were measured at, so daily.sh rebuilds THAT corpus rather than the default 100. Without it the daily job fetches a differently-sized corpus, check.sh compares its sweep against corpus.sweep_patched.passed anyway, and the fix agent's only way to go green is to overwrite the recorded evidence with whatever the different corpus gave \u2014 which is what happened to bash (ledger 492 packages, daily job 99, ledger duly rewritten down). 394 rather than a round number because that is what 263 projects expand to: a project contributes its Scala 3 line and its newest Scala 2 line, and 131 of the 263 have both.",
    "ranking_deviates_from_java_and_why": {
      "measured_2026_08_12": "ecosyste.ms ranks Maven artifacts by how many public repositories depend on them, which for Scala is a decade-lagging metric. The top 4,000 Maven artifacts by that count contain ZERO _3 coordinates. org.apache.spark:spark-core_2.11 scores 8,772; org.typelevel:cats-core_3 scores 2; dev.zio:zio_3 scores 1. Taken literally the list resolves to Spark 2.4.8, Kafka 2.4.1, Akka 2.5.32 and Flink 1.14 \u2014 2016-era Scala 2.11.",
      "consequence_if_reused_unchanged": "a Scala-3-era grammar would be swept against Scala 2.11 only, and the dialect split this language was queued for would never appear in a single verdict.",
      "what_lang/scala.rs_does_instead": "the ranking decides WHAT IS POPULAR and the coordinate decides WHICH DIALECT TO SWEEP: rank by dependent repositories, collapse a project's cross-built lines (foo_2.11, foo_2.12, foo_3 are one project), then emit that project's newest published Scala 2 line and its Scala 3 line. Everything else \u2014 resolve, sources-jar naming, the fetch driver's skip-on-404 \u2014 is java's, unchanged.",
      "result": "263 projects -> 394 coordinates. The naive ranking produced 0 _3 in its top 4,000.",
      "ties_are_not_ordered_stably": "Found 2026-08-16, and it is why `rank` sorts rather than taking the API's order. ecosyste.ms does not break ties, and Scala's ranking has a long tie plateau \u2014 every org.scala-sbt module sits at exactly 354 dependent repos. Two runs three days apart, same k, produced different members from position 70 of 94 onward: librarymanagement-core and sbinary in one, gigahorse-core and sjson-new-core in the other. A corpus that reshuffles under a ledger whose numbers were measured on the old one is the same evidence-corrupting failure `fetch_limit` guards against, arriving by a different route, so rank now stops when the top k is SETTLED (a page's smallest count has dropped below k-th place) rather than merely reached, and sorts by count then coordinate. Verified: two consecutive runs at k=60 now produce byte-identical lists.",
      "scan_window": "150 pages of 100 artifacts. Scala is sparse on Maven Central, so this is less a limit than a definition: every Scala project among the top 15,000 artifacts by dependent repositories. Yield per page declines but does not cliff \u2014 2.05 projects/page over pages 1-60, 2.17 over 61-90, 1.37 over 91-120, 1.13 over 121-150 \u2014 so the cutoff is a judgement about where the marginal page stops paying, not a point where the data runs out.",
      "popularity_is_the_project's": "both lines of a cross-built project carry the project's dependent-repository count, so `downloads` here is a per-project figure shared by up to two coordinates, and is a dependency-graph proxy rather than traffic (Maven Central publishes no download counts). Unlike the crates.io and npm rankings in this repo."
    },
    "sweep_upstream": {
      "passed": 17600,
      "failed": 65
    },
    "sweep_patched": {
      "passed": 17624,
      "failed": 41
    },
    "noise": 0,
    "noise_note": "Every one of the 65 upstream failures was adjudicated VALID by scalameta under the file's declared dialect \u2014 65 grammar gaps, 0 corpus noise. It held at 94 coordinates and it still holds at 394, which is the result worth having: the same corpus judged entirely as Scala3 would have produced false `invalid` verdicts for 8.6% of files, each of which could have hidden a gap.",
    "classify_is_scala_only_and_that_was_measured": "`.sbt` and `.sc` are Scala too, and tree-sitter.json claims `.sbt`, so classify could plausibly take them \u2014 ruby's equivalent breadth pass found 1,586 files. Measured across all 394 sources jars: 7,589 .scala, 2,992 .java (correctly excluded), and ZERO .sbt or .sc. Build definitions do not ship inside a sources jar, so widening classify would add nothing here and would add a file whose dialect nothing in the corpus can declare. Recorded because \"we checked and it was empty\" is worth as much as a patch, and stops the next person re-deriving it.",
    "remaining_gaps": 41,
    "remaining_gaps_note": "41 files in 30 clusters, all adjudicated valid. The largest are `extension_definition > ERROR(case)` (3 files, the piece of patch 0004's family deliberately left out), `block > ERROR(=> null_literal)` and `block > ERROR(open_modifier)` (2 each). Nothing here is a defect in the numbers above."
  },
  "patches": [
    {
      "id": 1,
      "title": "treebank redistribution notice",
      "file": "patches/0001-treebank-redistribution-notice.patch",
      "files": [
        "README.md"
      ],
      "kind": "packaging",
      "origin": "treebank"
    },
    {
      "id": 2,
      "title": "treebank crate identity",
      "file": "patches/0002-treebank-crate-identity.patch",
      "files": [
        "Cargo.toml",
        "Cargo.lock"
      ],
      "kind": "packaging",
      "origin": "treebank"
    },
    {
      "id": 3,
      "title": "Scala 2 `enum` as a term name",
      "file": "patches/0003-scala-2-enum-as-a-term-name.patch",
      "files": [
        "grammar.js",
        "test/corpus/definitions.txt"
      ],
      "origin": "treebank",
      "evidence": {
        "repro": "def qualifyEnum(enum: Enum[_]): String = enum.getClass.getCanonicalName",
        "first_seen": {
          "package": "org.apache.flink:flink-scala_2.12",
          "version": "1.20.5"
        },
        "why": "`enum` is a Scala 3 keyword and an ordinary Scala 2 term name. Upstream already makes this allowance for import paths (`_namespace_path_segment`, commented 'Scala 3 keywords that are valid identifiers in Scala 2 sources'); Scala 2 sources also declare and use such names as terms, which upstream rejected. The patch extends the same allowance to parameter names and to expression position, and declares the one conflict it creates: after `enum`, an identifier is either the enum's name or a Scala 2 postfix application of a value called `enum`, and only the rest of the construct settles it.",
        "scope": "`enum` only. `export` is its twin in upstream's path rule and was tried, but no corpus file uses it as a term and admitting it in expression position costs a second GLR conflict against `export_declaration` \u2014 so it was dropped rather than shipped on symmetry.",
        "sweep_before": {
          "passed": 7560,
          "failed": 29
        },
        "sweep_after": {
          "passed": 7566,
          "failed": 23
        },
        "sweep_corpus": "the 94-coordinate / 7,589-file corpus of 2026-08-13, which is what existed when the patch was written. On the current 394-coordinate corpus the same patch is 17600/65 -> 17608/57, so it fixes 8 files rather than 6.",
        "fixed_files": 6,
        "grammar_tests": "272 -> 273 upstream corpus tests, all passing"
      }
    },
    {
      "id": 4,
      "title": "Scala 2 names that Scala 3 made keywords",
      "file": "patches/0004-scala-2-names-scala-3-made-keywords.patch",
      "files": [
        "grammar.js",
        "test/corpus/definitions.txt"
      ],
      "origin": "treebank",
      "evidence": {
        "repro": "val enum = enumOf(obj)  //  and  @JsonSerialize(using = classOf[X])",
        "first_seen": {
          "package": "com.twitter:chill_2.13",
          "version": "0.10.0"
        },
        "why": "Patch 0003's problem in the two positions it did not reach. `enum` is a Scala 3 keyword and an ordinary Scala 2 term name, and 0003 admitted it as a parameter name and in expression position; Twitter's chill and Flink also BIND it (`val enum = ...`), which goes through _definition_pattern. `using` is the same story with a different keyword: Spark and Delta both pass it as a named argument, `@JsonSerialize(using = classOf[...])`, where the Scala 3 `(using expr)` clause claims the token and leaves the `=` nowhere to go. That one needs a declared conflict, because `(using x)` and `(using = x)` are told apart by a token that can be an arbitrary expression away.",
        "not_included": "`extension` as a pattern name (`case extension: CatalogExtension =>`, 3 files) belongs to this family and is NOT here. It is already a scanner-lexed _soft_identifier, so the alias trick does not apply and it needs a conflict against extension_definition; it was tried, cascaded, and was worth less than the cluster below.",
        "sweep_before": {
          "passed": 17608,
          "failed": 57
        },
        "sweep_after": {
          "passed": 17613,
          "failed": 52
        },
        "fixed_files": 5,
        "grammar_tests": "273 -> 274 upstream corpus tests, all passing"
      }
    },
    {
      "id": 5,
      "title": "A newline ends a bare Scala 2 declaration",
      "file": "patches/0005-a-newline-ends-a-bare-scala-2-declaration.patch",
      "files": [
        "grammar.js",
        "src/scanner.c",
        "test/corpus/definitions.txt"
      ],
      "origin": "treebank",
      "kind": "scanner",
      "evidence": {
        "repro": "trait A { def f(t: Throwable)\\n  val x = 1 }",
        "first_seen": {
          "package": "org.apache.flink:flink-streaming-scala_2.12",
          "version": "1.20.5"
        },
        "why": "The largest cluster in the corpus, and it is not what it looks like. Scala 2 procedure syntax is NOT unsupported: `def f(t: T) {}` parses, and so does a lone `def f(t: T)` as a template's last member. What failed was a bare declaration with ANY member after it, because Scala lets a `def` continue onto the next line in three places \u2014 before another parameter list, before the result type, before the body \u2014 and a bare declaration ENDS at that same newline. Both readings were live at one token, the grammar preferred continuing (prec.right on _function_constructor plus a trailing optional semicolon), and the ending reading was therefore unreachable.",
        "why_the_scanner": "The parser cannot decide this: what follows the newline may be an arbitrary distance away, so no fixed lookahead settles it, and every grammar-level attempt failed. Declaring the conflict is reported unnecessary because the precedence already resolved it; swapping prec.right for prec.dynamic moves the failure to `def this` and cascades; deleting the repeat's optional semicolon breaks upstream's own tests 54 and 55, which is how its load-bearing role was confirmed. The lexer CAN decide it, because the answer is the very next character: only `(`, `[`, `:`, `=` or `{` continues a def. So the continuation gets its own external token and the ending newline stays an ordinary AUTOMATIC_SEMICOLON. The two can no longer be confused, and the grammar needs no conflict at all.",
        "blast_radius": "valid_symbols keeps it narrow \u2014 DEF_CONTINUATION is only valid inside a def's signature or before its body, so no other newline in the language can see it. Checked in the other direction too: four new negative-corpus files probe exactly this area (a def whose `=`, `:` or `(` opens the next line with nothing after it, and a def with no name), all rejected by scalameta under BOTH dialects and by the grammar.",
        "sweep_before": {
          "passed": 17613,
          "failed": 52
        },
        "sweep_after": {
          "passed": 17624,
          "failed": 41
        },
        "fixed_files": 11,
        "grammar_tests": "274 -> 275 upstream corpus tests, all passing"
      }
    }
  ],
  "negative_corpus": {
    "files": 18,
    "note": "Every file is rejected by scalameta under BOTH Scala213 and Scala3, not just one \u2014 which matters here more than for a single-dialect language. tree-sitter-scala is ONE grammar for TWO languages, so the grammar is deliberately the union of Scala 2 and Scala 3 while the oracle is per file. A file that only one dialect rejects is therefore not a strictness test at all: the grammar is supposed to accept it. `def f(using ) = 1` was cut from the battery for exactly that reason. Four of the eighteen were added with patch 0005 to probe the scanner change specifically: a def whose `=`, `:` or `(` opens the next line with nothing after it, and a def with no name."
  },
  "docs_note": "Scala 2 and Scala 3 both parse: one grammar, dialect declared per package"
}
