diff --git a/grammar.js b/grammar.js
index 2e625b4..3b336e9 100644
--- a/grammar.js
+++ b/grammar.js
@@ -119,7 +119,7 @@ module.exports = grammar({
         $._multiline_basic_string_end,
       ),
     escape_sequence: $ =>
-      token.immediate(/\\([btnfr"\\]|u[0-9a-fA-F]{4}|U[0-9a-fA-F]{8})/),
+      token.immediate(/\\([betnfr"\\]|x[0-9a-fA-F]{2}|u[0-9a-fA-F]{4}|U[0-9a-fA-F]{8})/),
     _escape_line_ending: $ => token.immediate(seq(/\\/, newline)),
     _literal_string: $ =>
       seq(
diff --git a/test/corpus/tour.txt b/test/corpus/tour.txt
index 2e2c1e5..c87905a 100644
--- a/test/corpus/tour.txt
+++ b/test/corpus/tour.txt
@@ -374,3 +374,25 @@ ldt3 = 1979-05-27T07:32
   (pair
     (bare_key)
     (local_date_time)))
+
+=====================================
+String: \e and \x escapes (TOML 1.1)
+=====================================
+
+esc = "\e There is no escape! \e"
+hex = "\x20 \x7f \x00"
+
+---
+
+(document
+  (pair
+    (bare_key)
+    (string
+      (escape_sequence)
+      (escape_sequence)))
+  (pair
+    (bare_key)
+    (string
+      (escape_sequence)
+      (escape_sequence)
+      (escape_sequence))))
