diff --git a/grammar.js b/grammar.js
index 1168d4d..f5b5b5b 100644
--- a/grammar.js
+++ b/grammar.js
@@ -32,6 +32,12 @@ const PREC = {
   SUBSCRIPT: 17,
 };
 
+// C99 6.7.2: the complex types are written with a second specifier keyword
+// beside float/double — `_Complex float`, `double _Complex`, `long double
+// _Complex` — so they belong with signed/unsigned/long/short rather than in
+// primitive_type. __complex__ is GCC's spelling, accepted in every dialect.
+const COMPLEX_SPECIFIERS = ['_Complex', '__complex__', '_Imaginary'];
+
 module.exports = grammar({
   name: 'c',
 
@@ -644,6 +650,7 @@ module.exports = grammar({
           'unsigned',
           'long',
           'short',
+          ...COMPLEX_SPECIFIERS,
         )),
         field('type', optional(choice(
           prec.dynamic(-1, $._type_identifier),
@@ -654,6 +661,7 @@ module.exports = grammar({
           'unsigned',
           'long',
           'short',
+          ...COMPLEX_SPECIFIERS,
         )),
       ),
       seq(
@@ -662,6 +670,7 @@ module.exports = grammar({
           'unsigned',
           'long',
           'short',
+          ...COMPLEX_SPECIFIERS,
         )),
         repeat($.type_qualifier),
         field('type', optional(choice(
@@ -673,6 +682,7 @@ module.exports = grammar({
           'unsigned',
           'long',
           'short',
+          ...COMPLEX_SPECIFIERS,
         )),
       ),
     ),
diff --git a/test/corpus/types.txt b/test/corpus/types.txt
index 6d2d19a..ecbb082 100644
--- a/test/corpus/types.txt
+++ b/test/corpus/types.txt
@@ -78,3 +78,40 @@ void f(unsigned long v2);
     (function_declarator
       (identifier)
       (parameter_list (parameter_declaration (sized_type_specifier) (identifier))))))
+
+========================================
+Complex types
+========================================
+
+_Complex double a;
+double _Complex b;
+long double _Complex c;
+__complex__ float d;
+struct s { _Complex float cf; };
+
+---
+
+(translation_unit
+  (declaration
+    type: (sized_type_specifier
+      type: (primitive_type))
+    declarator: (identifier))
+  (declaration
+    type: (sized_type_specifier
+      type: (primitive_type))
+    declarator: (identifier))
+  (declaration
+    type: (sized_type_specifier
+      type: (primitive_type))
+    declarator: (identifier))
+  (declaration
+    type: (sized_type_specifier
+      type: (primitive_type))
+    declarator: (identifier))
+  (struct_specifier
+    name: (type_identifier)
+    body: (field_declaration_list
+      (field_declaration
+        type: (sized_type_specifier
+          type: (primitive_type))
+        declarator: (field_identifier)))))
