{
  "grammar": "c",
  "upstream": {
    "git_url": "https://github.com/tree-sitter/tree-sitter-c",
    "sha": "b780e47fc780ddc8da13afa35a3f4ed5c157823d",
    "version": "0.24.2",
    "sha_note": "commit target of tag v0.24.2 (also the default branch head at pin time); submodule pointers are commits, not annotated tag objects"
  },
  "generate_cli": "0.25.10",
  "generate_cli_note": "same pin as the other grammars: tree-sitter-cli 0.26.x ships broken Unicode identifier tables (see treebank-rust ledger)",
  "generate_dirs": [
    "."
  ],
  "generate_deps": null,
  "corpus": {
    "source": "Debian sid, source packages ranked by popcon (popularity-contest) installs aggregated per source package, filtered to packages whose sources.debian.org SLOC is >=2000 ansic and ansic >= cpp; upstream .orig.tar.* only (never the .debian.tar.* patch tarball); .c and .h files, C++ headers excluded by content",
    "source_note": "C has no registry, so popularity is borrowed. popcon counts machines, which is closer in kind to crates.io downloads than Java's dependent-repos proxy. The bias is explicit and it is the point: this corpus is the C that ships in a distribution \u2014 system libraries, daemons, autotools trees, GNU extensions \u2014 NOT trending C on GitHub, which is a different corpus and would give different gap numbers. The SLOC filter exists because popcon ranks everything Debian ships: without it the top of the list spends its bandwidth on LibreOffice (4.4M lines of C++ to 34k of C) and gcc-16 (no C at all).",
    "oracle": "tools/c-oracle \u2014 libclang 20.1.2, parse-only, verdict from clang's own diagnostic categories. THREE-VALUED: valid / invalid / indeterminate. See crates/treebank-c/ORACLE.md for what it does and does not claim; the short version is 'no syntax error, in GNU C, given these include paths', NOT 'this compiles'. Lang::validate collapses indeterminate to false, so gap_files is a FLOOR and noise_files mixes 'reference rejected it' with 'could not tell'; the full split is in corpus/c/oracle-verdicts.json and on the sweep's stderr.",
    "fetched": "2026-08-08 (popcon + sid Sources snapshot of that date)",
    "packages": 20,
    "files": 39928,
    "files_note": ".c and .h from the top 20 C source packages by popcon; 365 of 12,767 headers dropped as C++ (unguarded namespace/template/class, comments and __cplusplus-guarded regions excluded from the scan)",
    "sweep_upstream": {
      "passed": 21991,
      "failed": 17937,
      "gap_files": 5629,
      "noise_files": 12308,
      "clusters": 924
    },
    "oracle_split": {
      "valid": 5629,
      "invalid": 302,
      "indeterminate": 12006,
      "note": "of the 17,937 failing files. INDETERMINATE OUTNUMBERS VALID 2.1:1, so gap_files is a floor and noise_files (12,308) is mostly 'could not tell', not 'reference rejected it' \u2014 only 302 files were positively rejected. 95%+ of indeterminates carry an unresolved include; the single most common missing header is the configure-generated config.h, which no static include path can supply."
    },
    "oracle_flags": "-std=gnu17 -ferror-limit=0 -w, -iquote<every header-bearing dir in the package and its ancestors>, -I<package root, include/, inc/, src/, lib/>",
    "oracle_flags_note": "-iquote rather than -I for package-internal dirs is load-bearing and was measured: with -I the package's private replacements for system headers shadow the real ones (glibc string/string.h answering <string.h>) and resolution gets WORSE. Full pilot, 17,868 failing files: conventional -I = 4578 valid/13144 indeterminate; every header dir with -I = 4060/13664; every header dir with -iquote = 5651/11917.",
    "top_gap_clusters": [
      {
        "signature": "expression_statement > MISSING ;",
        "valid": 926,
        "files": 3455,
        "class": "statement macro followed by a block, e.g. list_for_each(li, &q->ifaces) { ... }"
      },
      {
        "signature": "preproc_ifdef > MISSING #endif",
        "valid": 763,
        "files": 912,
        "class": "extern \"C\" brace asymmetry: `{` inside one #ifdef __cplusplus and `}` inside another. Preprocessor-inherent, same shape as the C# asymmetry; not a fixable grammar bug"
      },
      {
        "signature": "function_definition > ERROR(identifier)",
        "valid": 456,
        "files": 1383,
        "class": "undefined macro in declaration position, e.g. _INLINE_ void __list_add(...)"
      },
      {
        "signature": "declaration > MISSING ;",
        "valid": 449,
        "files": 1557,
        "class": "same family"
      },
      {
        "signature": "declaration > ERROR(identifier)",
        "valid": 290,
        "files": 1406,
        "class": "macro in declaration position, e.g. THREAD_LOCAL int adjustment = 0;"
      },
      {
        "signature": "argument_list > ERROR(identifier)",
        "valid": 168,
        "files": 1782,
        "class": "type as a macro argument, e.g. list_entry(a, struct file_element, file_list)"
      }
    ]
  },
  "patches": [
    {
      "id": 1,
      "title": "treebank redistribution notice",
      "file": "patches/0001-treebank-redistribution-notice.patch",
      "files": [
        "README.md"
      ],
      "kind": "packaging",
      "origin": "treebank; README warning that this tree is a generated, patched redistribution (treebank.dev), applied first so every materialized tree carries it"
    }
  ]
}
